name: Linux

# Linux x86-64 (also what the Steam Deck runs).
on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: linux.yml-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    uses: ./.github/workflows/test.yml
    with:
      os: ubuntu-latest
