# GPU Renderer Goal Prompt

Improve VERA20k’s GPU rendering system so it matches active-retail `gamemd.exe` more closely and scales well with large unit counts. Preserve the existing `wgpu` architecture and simulation/render separation. First inspect the complete current rendering path and identify the most important verified visual or performance problem. Use `C:\Users\enok\Documents\OpenTS` as a readable secondary reference, but verify every material behavior against active-retail `gamemd.exe`, retail assets and native captures before implementing it. Fix one bounded mechanism at a time—such as draw order, per-pixel depth, bridge overlap, shadows, palettes, blending, clipping, culling or batching. Preserve what already matches, replace unsupported approximations, and never trade visual correctness for speed. Validate each change with focused tests, GPU captures and measured performance. Give every completed mechanism to a fresh read-only critic, fix its largest finding, and repeat until it passes. Afterward, audit the complete renderer for regressions and remaining high-impact gaps. Proceed autonomously and follow `AGENTS.md`.
