# Movement: retail acceptance and increment ledger

Goal: VERA20k movement looks and works like retail Yuri's Revenge. This page
fixes what "like retail" means for movement, how each claim is evidenced, and
which increments have landed. It is a working ledger, not a parity certificate:
every status below cites the evidence it rests on and nothing more.

## Acceptance

A movement behaviour is accepted when an **ordinary, undisabled player order on
a retail map** produces the retail outcome, and the native mechanism behind it is
established from `gamemd.exe` (body, active caller, retail data). The bar is the
one frozen in [bridge-movement-matrix.md](bridge-movement-matrix.md) §0: synthetic
grids are regression ratchets, never parity; a gate whose own provenance says
"gamemd has no equivalent" is a divergence even while it produces no wrong pixel.

Player-visible criteria, in priority order (frequency × visibility):

| # | Criterion | Retail behaviour to match | Evidence instrument |
|---|---|---|---|
| A1 | Order admission | **Criterion text corrected 2026-09-16 - it described a refusal mechanism that does not exist.** Retail refuses almost nothing: `+0x1AC` code 7 picks the **cursor**, not admission (`TechnoClass::What_Action 0x00700600` calls `Can_Enter_Cell(cell,-1,-1,0,1)` at `0x00700B75` and branches `CMP EAX,1 / JLE`, so `<= 1` is MOVE and `>= 2` is NOMOVE), and ACTION_NOMOVE **still issues a Move** - the jump tables at `0x004D8270`/`0x004D8244` send actions 1 and 2 to one handler, action 1 only adding the destination flash before falling into `CALL 0x004DE1D0` at `0x004D806F`. Zone failure does not refuse either. So the thing to match is not which orders are refused but **which cell a doomed order is retargeted to**. | Retail-map `Command::Move` harness (`movement_bridge_retail_tests.rs`), Ghidra `+0x1AC` and `0x004DE1D0` bodies |
| A2 | Route choice | A* over `Can_Enter_Cell` codes with the native cost table `0x0081870C`, hierarchy precheck, smoothing `0x0042B420` | `spatial_oracle.path_entry`, A* trace lanes, retail-map routes |
| A3 | Drive turning and tracks | Turn-table curves, ROT, residual budget, `Accelerates` ramp per `Process_Drive_Track 0x004B0F20` | `locomotor_track_*` oracles, `drive_track_tests.rs` |
| A4 | Column following | Followers chain onto an occupant in transit (`0x0073FA2C..FA7C`, code 2) instead of stopping **or scattering** - `Scatter_Objects 0x00481670` has exactly four call sites in `Process_Movement` (`0x004B2DC0`, `0x004B327D`, `0x004B393A`, `0x004B4437`) and code 2 jumps past all four | Retail-map convoy run; Ghidra |
| A5 | Blocked recovery | Retry counter, `CloseEnough`, scatter of blockers (`Scatter_Objects 0x00481670`, ten-entry cap) | Ghidra; retail-map blocked run |
| A6 | Infantry stepping | Sub-cell slots, `Walk` head/boundary/completion at `0x0075AEC0` family | `walk_*` oracles (consumed), retail-map runs |
| A7 | Crush | Crusher over `Crushable` infantry, sandbags and fences; victim lifecycle inside the crusher's slot | Ghidra; retail-map run |
| A8 | Height, slopes, bridges | Ground/deck height, ramp tilt, slope transitions; under-span and over-span routing | `ramp_height_oracle`, retail bridge matrix |
| A9 | Hover motion | Own XY integrator and vertical bob, not the Drive track | Ghidra `HoverLocomotionClass`; retail run |
| A10 | Scale | 20,000 units move without per-object whole-map work | `sim-phase-profiler`, hash-equal regression |
| A11 | Jumpjet flight | Cruise, turn and height profile of `JumpjetLocomotionClass` (`Update_Coordinates_And_Altitude 0x0054D0F0`, states `0x0054B980..0x0054CA90`): speed ramp and zones, turn at `JumpjetTurnRate=`, cruise-height dips and terrain look-ahead, arrival below 20 leptons, takeoff, hold and landing | `spatial_oracle.jumpjet_flight`, `spatial_oracle.jumpjet_states`, `spatial_oracle.jumpjet_coordinates`; Ghidra |
| A12 | Wall-clock pace | At a given `GameSpeed=`, the same observable pace as gamemd: `Main_Tick` throttles each loop to `speed_byte × 16 ms` (byte 1 → ~62.5 logic frames/s; byte 0 uncapped), and every frame-counted system scales with it | `LocalFramePacer::should_admit` against the `0x0055E160` / `0x006C8C40` throttle; Ghidra. **No measurement instrument exists on either side** — see the pace residual. **Trigger:** any display below about 63 Hz, which is most of them - `render/gpu.rs` pins `PresentMode::Fifo` and frame admission is reachable only from the render pass with no catch-up loop, so admission only ever permits a frame and never replays a missed one. **Frequency: continuous**, a uniform deficit across every frame-counted system: about 4 percent at 60 Hz and 2x at 30 fps, appearing as slow motion rather than gamemd's frame-dropping |

## Instruments

- **Retail-map harness:** `src/sim/movement/movement_bridge_retail_tests.rs`
  drives ordinary orders on `Hills.mmx`, `BayOPigs.mmx`, `Deadman.mmx` and records
  per-tick cell, z, on-bridge and occupancy. Ignored in CI; needs `config.toml`.
- **Native oracles:** `tools/spatial_oracle/*` (Unicorn, binary
  `1cdd1180…4298c`), see [tools/native_oracle.md](../../tools/native_oracle.md).
  No motion-over-time oracle exists; the executable capture programme is
  `BLOCKED` ([capture report](../research/GROUND_MOVEMENT_EXECUTABLE_NATIVE_ORACLE_CAPTURE_REPORT.md)).
- **Ghidra:** `gamemd.exe` in `testProsjekt`; UnitClass vtable `0x007F5C70`
  (`+0x1AC` at `0x007F5E1C` → `0x0073F0A0`), InfantryClass vtable `0x007EB058`
  (`+0x1AC` at `0x007EB204` → `0x0051BF90`).

## Increment ledger

Each row lands as its own reviewed PR. Status words: `LANDED` (merged, retail
harness green), `OPEN` (not started), `IN PROGRESS`.

| Increment | Criterion | Status | Evidence |
|---|---|---|---|
| I1 Ground-plane routing beneath an intact high span for Drive and Hover (matrix T1-13 / T1-15) | A1, A8 | LANDED (PR #371) | `UnitClass::Can_Enter_Cell 0x0073F0A0` never calls `CheckCellPassability 0x004834A0` (xref list: CellRect, threat scan, placement, paradrop, overlay Mark, Jumpjet touchdown); it defers height legality to `CheckBridgeTraversal 0x004D9C60` (equal-level arm admits) and selects the ground list `+0xE4` at `0x0073F51A` for a path height within one of the cell level (`0x0073F0B7..F0E8`), reading the land row at `0x0073FAB5` only on that branch. Rust: `cell_entry.rs` class arm extended from Infantry to every ground-layer mover; `TerrainCostGrid::ground_cost_at` supplies the terrain's own row beneath a deck. |
| I2a Movement pass whole-map setup skipped on object turns that build no path | A10 | LANDED (PR #372) | `pass_may_build_paths` gates the blocker plane; NavCom re-aim scan scoped to the pass. Exact: suite identical (8964/0), two retail crossings 23.55 s → 8.78 s. |
| I2b Blocker plane cached across object turns | A10 | LANDED (PR #374) | Profile (400 MTNK on Hills, 150 frames, test profile, taken under concurrent build load so only the proportions are meaningful): blocker plane ≈27% of the frame, marker-peer snapshot ≈20%, per-pass owner block-set build ≈20%, all O(N) or O(W·H) per moving object's turn. The plane is a function of the cell-marked, non-dying, non-passenger objects (cell, list layer, category, foundation), each cell's terrain-object occupation and the retained wall plane. `MovementPassCache` on `Simulation` keys it on `OccupancyGrid::generation` (membership and layer), `EntityStore::dying_epoch` (every production `dying` writer notes it; a dying object stays marked until its terminal UnInit), `ResolvedTerrainGrid::mutation_epoch` (bumped in `cell_mut`, the one path of the single terrain-object writer; until 2026-09-15's audit fix the bump sat only in the `cfg(test)` body, so a game build reused the plane with a phantom neighbour source after a tree died until any unit changed cell — `terrain_object_occupation_writer_moves_the_mutation_epoch` now pins the shipped path) and `OverlayGrid::mutation_epoch` (bumped by the public and wall-transaction mutators). Debug builds rebuild and compare on every reuse, so the test profile gains nothing and every test exercising a reuse is an exactness check; the full suite, the death-window unit test and the 400-mover run hold. A fresh critic traced every input's writers; its dying-window and un-bumped-mutator findings are applied. Measured under identical machine conditions (test profile, `scale_benchmark_many_movers_on_hills`, 400 movers, 150 frames): 52.8 ms/frame without the cache, 42.2 ms with it (cross-check off), 54.4 ms with the debug cross-check (each reuse rebuilds and compares). Earlier readings taken while other builds ran (115 → 39 ms) overstated both sides and are withdrawn; the remaining per-mover terms below dominate the frame. Not exact-cacheable and therefore left per turn: the owner block sets and the marker-peer snapshot read `movement_target`, `foot_occupation_enabled`, facing and sub-cell position, none of which bump a generation; their native shape is a live cell-list walk per mover, which needs the mover taken out of the store for its turn (next design step). |
| I3 Ally-occupant arm of `UnitClass::Can_Enter_Cell`: in-transit skip and head-on exit | A4, A5 | LANDED (PR #373) | Native `0x0073FA2C..FA7C`: an in-transit (`Foot+0x6B6 == 0`) or infantry ally is skipped unless its locomotor slot `+0xA4` answers true (Drive `0x004B4B00` / Ship `0x006A4130` `Can_Use_Track`; base `0x004B6640` always false); head-on exit `0x0073F8D4..FA26` (opposed octants, `Sqrt_Approx` distance ≤ `0x1FF`, bearing in the mover's octant → 7). Rust: `drive_track::occupant_can_use_track` (parity demonstrated: `tools.spatial_oracle.locomotor_can_use_track`, 12,320 cases), `cell_entry::classify_blocker` and `bump_crush::build_entity_block_sets` skip rule, `cell_entry::head_on_exit` on the live walk and the Drive selection lane (`MovingAllyOccupant` record). The two-tank head-on fixture's floor re-derived to the measured 116 leptons (see `repro_two_moving_vehicles_pass_through_each_other`); that number is a VERA ratchet, not a native bound. Residuals: the "moving" test is still `movement_target` rather than native's NavCom/rotating/`Is_Moving` triple; the per-owner head-on record refreshes only on occupancy-generation change. The residual first recorded here ("VERA re-sets `foot_occupation_enabled` on every crossing; Ship/Hover never clear it") was traced in I3b: Drive and Ship production turns run `track_host.rs`, which already clears the enable at the first paid point and restores it at the terminal point (`0x004B161A` / `0x004B1FEF`, Ship `0x006A0CDA` / `0x006A1632`) and gates the raw mark on it like `AddContent`; the re-setting crossing in `cell_arrival.rs` is reachable only from the non-suspending test entry (`tick_movement_with_grids`). Hover was the real gap. |
| I4 Crushable overlays fall to any crusher, with the overlay's `CrushSound=` | A7 | LANDED (PR #375) | `UnitClass::PerCellProcess 0x0073AFD4..B074` (disassembly read 2026-09-15): `Crusher=` (`+0xD28`) or ability 0x11 → overlay present → `Crushable=` (`+0x22D`) or (`Wall=` `+0x2A8` and **`MovementZone`** `TechnoTypeClass+0x5B4` `== 0xC`, i.e. `CrusherAll` - this row said `LocomotorType` until the A7 examination settled the field: `ReadINI 0x0071605E` reads key "MovementZone" (`0x008431C8`) through `CCINIClass::ReadMovementZone 0x00474E40` against the name table `0x0081BA88`, where index 12 is `CrusherAll` and index 6 is `Subterannean`, matching the adjacent `CMP EAX,6 / SETZ`. `movement_path.rs:108` and `walk_path.rs:102` already read it correctly; I9a's reading was correct too) → `VocClass::PlayAt(OverlayType+0x1F0 CrushSound)` at the unit → `DestroyOverlay(-1)` → `RockingForwardsPerFrame += 0.02`. VERA's `apply_wall_crush_on_driveover` previously required `Wall=` and Drive for everything, so fences and sandbags (`CAFNCB/CAFNCW/CAFNCP/GASAND`, `Crushable=yes`) survived a Robot Tank and every crush was silent. Now: crushable clause added; `OverlayTypeFlags::crush_sound` parsed; `SimSoundEvent::WallCrushed` → `GameSoundEvent::WallCrushed` positional Voc. Residuals: sweep runs post-movement rather than in the per-cell entry callback; rocking tilt has no renderer; crush-clamp byte `Foot+0x6B5` unmodelled; ability 0x11 (`VeteranCrusher=`/`EliteCrusher=`) reaches only the bridge-repair and production-spawn admissions (`bridge_repair_admission.rs`, `production_spawn.rs`), not this sweep (no stock grant). Follow-up recorded by the critic: the `cell_entry.rs` header (lines ~77-92) and `cell_rect.rs` comments still claim the overlay model carries no `Crushable` authority and that miners stop at sandbags; `OverlayTypeFlags::crushable` and the `CRUSHABLE` zone class contradict that prose. |
| I3b Hover occupation enable (`+0x6B6`) lifecycle | A4, A9 | LANDED (PR #376) | `HoverLocomotionClass::Move 0x00514310` (decompile and disassembly read 2026-09-15; re-read by the increment's critic): speed is `ftol` of the current speed (`0x00514383`); a translating frame (speed > 0) with `IsCellOccupationEnabled` (`+0xC0` → `0x0041C070`) true and a live Head_To calls `+0xF4` (`UnitClass 0x00744210`, raw 0x20 clear) and zeroes `+0x6B6`/`+0x6B7` at `0x005147CC..7DF`; the arrival arm (`Sqrt_Approx` distance to Head_To ≤ speed, `0x0051450F`) sets `+0x6B6 = 1` at `0x0051451E` at **every reached head**, and the same call re-clears it only when the next step is accepted; every next-step call (`FUN_00514F70`, prologue `0x00514F70..0x00514FB2`) first releases the reached cell's raw claim (`+0xF4`) and invalidates Head_To, and a refused step returns with the enable still set (code 7 at `0x00514711`, `+0x684` bit 7 clear), so natively only the enable survives a refusal. `Stop_Moving 0x00516320` leaves Head_To untouched, so a stopped hover still reaches the arrival arm. VERA's hover never touched the flag, so a moving hover blocked every ally (code 2) where retail skips it through the in-transit arm. Rust: `movement_tick.rs` hover arm clears the enable and the owner-plane claim on a translating frame, the deferred-refusal tail restores the enable (and, as VERA's representation, its owner-plane claim) while the hover waits at its boundary, `finalize_finished_entities` restores them at arrival; a hover whose target is dropped mid-transit re-enables on its next own turn (`restore_stopped_hover_occupation_enable`; VERA timing, native restores at the Head_To cell centre it still glides to). Tests: `hover_mover_is_in_transit_from_first_translating_frame_until_arrival`, `hover_owner_plane_claim_is_absent_in_transit_and_present_at_arrival`, `hover_refused_next_step_re_enables_occupation_while_it_waits`, `stopped_hover_left_in_transit_re_enables_occupation_on_its_next_turn`, `classify_blocker_skips_a_hover_ally_only_while_it_is_in_transit`. Residuals: `+0x6B7` unmodelled; the raw 0x20 plane is not moved by hover crossings at all (pre-existing; readers are the raw-occupation rect checks and the 5x5 marker scan); VERA's hover integrator translates on sub-lepton speeds where native (`ftol`) holds still, so the gate is "will translate"; VERA's turn-stall (I4b) holds the enable set during a >45° turn from rest where native has no stall; a refused VERA hover waits at the cell boundary, native at the cell centre, and VERA restores the enable after the deferred response where native sets it before the next-step admission (no flag or plane reader sits in that window today). Adjacent, not I3b: native code 7 from the arrival arm drops the path (`+0x5E0 = -1`), starts the path delay and re-paths at once through `FUN_005164D0`; VERA keeps the path and waits at the boundary (blocked-response mechanism). |
| I9a Crushable walls admit only crushers | A1, A7 | LANDED (PR #378) | `UnitClass::Can_Enter_Cell` wall arm `0x0073F3D0..F4F9` (disassembly and decompile read 2026-09-15): `Crushable=` (`+0x22D`) with the type's `Crusher=` (`+0xD28`) or ability 0x11 enters (code unchanged against a non-allied wall, `max(code,4)` against an allied one, `0x0073F46E..F4F5`); `MovementZone=CrusherAll` (`+0x5B4 == 0xC`) enters any `Wall=` (`0x0073F455..F46C`); everyone else takes the weapon route: no primary weapon (`+0x2AC` → `0x00701120`) → 7, warhead `Wall=` (`+0x144`) or `Wood=` (`+0x147`) with overlay `Armor` 6 → 4 (allied) / 5 (enemy), else 7. `InfantryClass 0x0051BF90` has no crusher route: `Wall=` overlay → primary weapon with warhead `Wall=` (`FUN_00772AC0`) → `5 - ally`, else 7. `CheckCellPassability 0x00483598..5C8` (placement leaf) admits Crusher/AmphibiousCrusher on `+0x22D`. VERA before: a crushable overlay reduces to zone class 1 (`overlay_reduced_zone_type`, matching `RecalcZoneType`), the path grid blocks only WALL/IMPASSABLE and the class arm tested WALL, so sandbags and fences admitted every mover with a passable speed row. Now: `CanEnterCellContext::mover_is_crusher` (the `Crusher=` flag; A* passes its crush-capability option, a stock superset by `OmniCrusher=`) and zone class 1 gate the arm: crusher or CrusherAll enters, infantry and non-crusher vehicles are hard-blocked. Stock check: the only `Crushable=yes` overlays in `rulesmd.ini` are `[GASAND]`, `[CAFNCB]`, `[CAFNCP]`, `[CAFNCW]`, all `Wall=yes`; 29 types carry `Crusher=yes`. Tests: `crushable_wall_admits_only_crushers_and_crusher_all`, `astar_routes_non_crushers_around_a_sandbag_line_and_crushers_through_it`. Residuals (I9b): the weapon route's 4/5 codes have no producer, so a wall-capable non-crusher is refused where retail routes through at 60x/20x and shoots — in stock that is the IFV (`[FV]`, `HoverMissile` → `HE`) and the Brute (`[BRUTE]`, `Punch` → `Battering`); every infantry rifle and the other non-crusher vehicles answer 7 natively and match. The allied-wall 4 for crushers is unmodelled. Ability 0x11 (`VeteranCrusher=`/`EliteCrusher=`) is modelled only in the bridge-repair and production-spawn admissions; `CrushCapability::of`, and so every path search and the crossing, reads the static `Crusher=`/`OmniCrusher=` flags, which promotion never updates. No stock type grants it. A* keys the arm on its crush-capability option. Until I9c three production repaths (segment continuation, process-entry and the walk path request in `movement_tick.rs`) derived that option from MovementZone (`Crusher`/`AmphibiousCrusher`/`CrusherAll`) instead of `Crusher=`/`OmniCrusher=`, so after I9a a `Crusher=yes` tank on any non-crusher zone (Normal, Destroyer, AmphibiousDestroyer, Amphibious, Water: 22 stock types) detoured around a sandbag line on those repaths while its initial order and the crossing crossed it; I9c makes every production search read `CrushCapability::of`. Remaining: the crossing passes `regular_crusher` alone, stock-equivalent (only `[BFRT]` is OmniCrusher, and it is Crusher+CrusherAll); a modded OmniCrusher-only type would be A*-admitted and crossing-refused. The `scatter.rs` helpers pass `false` with no resolved terrain, so the wall arm never runs there and a sandbag is admitted for crusher and non-crusher alike. The spawn (`SpawnLike`) caller passes `false`, so a crusher's spawn onto a sandbag cell is refused where `CheckCellPassability` admits it. `cell_rect.rs` still cannot see a crushable wall as a wall. The infantry gate `(cell+0x11E >> 4) != DamageLevels` (`OverlayType+0x2A0`, art `DamageLevels=`) skips the wall arm on a fully destroyed wall stage; VERA removes a crushed overlay outright, so it has no such stage to skip. |
| I2c Live occupant classification per mover | A10 | OPEN | The per-mover owner block sets, marker-peer snapshot and building entry-skip map remain O(N) per object turn after I2b (they read `movement_target`, `foot_occupation_enabled`, facing and sub-cell, none of which bump a generation). Native shape is a live cell-list walk per mover from `UnitClass::Can_Enter_Cell`; VERA needs the mover taken out of the store for its turn to walk the lists directly. Profile facts in the I2b row. |
| I9c One crush authority for every path search | A1, A7 | LANDED (PR #380) | Regression from I9a, found while sizing I9b and widened by the increment critic. I9a keyed the crushable-wall arm on the search's crusher flag, but production searches derived that flag three different ways: initial orders (`world_commands.rs`) and blocked-step repaths (`movement_step.rs`, `movement_occupancy.rs`) passed `Crusher=` or `OmniCrusher=`; the proactive segment repath, the process-entry repath and the walk path request (`movement_tick.rs`) passed `omni_crusher \|\| MovementZone in {Crusher, AmphibiousCrusher, CrusherAll}`; and five first searches passed a literal `false`: the miner idle moves (`miner_system.rs`, `miner_dock_sequence.rs`), the chrono-miner staging destination (`miner_system.rs`), the factory rally move (`production_queue.rs`) and the attack-move/guard resume (`world_orders.rs`). Stock `rulesmd.ini` has 29 `Crusher=yes` types: 7 on crusher zones (BFRT, CMIN, CMON, HARV, HORV, SMIN, SMON) and 22 elsewhere (Normal: AMCV, HOWI, HTK, MGTK, MTNK, PCV, SMCV, TNKD; Destroyer: APOC, HTNK, LTNK, SREF, TTNK, UTNK, V3, XCOMET; AmphibiousDestroyer: ROBO; Amphibious: SAPC, YHVR; Water: CDEST, CRUISE, TUG). So after I9a a Grizzly past a sandbag line beyond its first segment stopped at the segment end, and a new tank sent to a rally point or any harvester on its idle move detoured around sandbags the crossing lets it cross. Native has no per-caller flag: every `Can_Enter_Cell` reads the type (`0x0073F438..F446` wall-arm crusher route; `0x0073FB2A..FB6C` occupant crush latch); `MovementZone=CrusherAll` is a separate wall-arm route (`0x0073F465`) that `cell_entry` keys on the zone. Rust: `CrushCapability::of(entity)` and `MoverSnapshot::crush_capability()` are the one authority; every production search above now reads it. Found while testing the fix: `find_layered_path_marker` built its A* options without the mover's `movement_zone`, so with no zone and no speed type `evaluate_shared_cell_leaf` returned on land passability alone and the Foot class arm (walls, crushable walls, I1's under-span ground admission) never ran in the layered fallbacks of `find_layered_path_zoned_marker_detailed` (water movers, a failed precheck with equal zones, same-zone unreachable, tube routes, any search without a zone grid). Every other search wrapper carried the zone; the five call sites now pass it, and the test-only `find_layered_path` passes `None` explicitly. `bump_crush::scatter_blocker` and the app cursor's cell-legality probe read it too (both read `Crusher=` alone before). The two `scatter.rs` helpers still pass `false`; the flag is inert there because they supply neither resolved terrain (the wall arm never runs, so a sandbag is admitted for any mover) nor a block map (no soft costs). Tests: `segment_repath_lets_a_crusher_tank_through_a_sandbag_line` (no locomotor, Normal zone; stops at `(24, 0)` without the fix), `process_entry_repath_lets_a_crusher_tank_through_a_sandbag_cell`, `attack_move_resume_lets_a_crusher_tank_through_a_sandbag_cell`; each fix has its own red run. Coverage limits: the miner idle moves, the chrono staging destination and the factory rally move have no dedicated test (the same one-line read as the tested attack-move resume). Threading the zone into the layered fallbacks also changes cases outside the crushable arm, untested here: water movers take the water-surface check instead of the land grid, and infantry take their zone's speed-type row, as every other search wrapper already did. Residual (pre-existing, now uniform): A* skips every entity soft-block cost for a crusher (`core.rs`, `!options.mover_is_crusher`) - moving allies (code 2), enemies (5, 20x) and stationary allies (6, 8x) - where native `AStar_compute_edge_cost 0x00429830` has no crusher term and a crusher only passes crushable non-allied victims (`0x0073FB67`). Trigger: any crusher path through occupied cells. Effect: crusher tanks plan straight through allied and non-crushable occupants at 1x. Frequency: most tank moves near other units. Before I9c the 22 non-crusher-zone types lost the exemption on their continuation repaths only; the survey backlog item #3 (A* occupant codes) owns the fix. |
| I9b Wall codes 4/5, A* wall cost and the wall-attack Override | A1, A2, A5 | OPEN (seam landed, producer unwired) | Native established (2026-09-15): codes above; `AStar_compute_edge_cost 0x00429830` prices 4/5 at 60x/20x from `0x0081870C`; `DriveLocomotionClass::Process_Movement 0x004B2630` dispatches codes 4/5 at `0x004B3A97..3BEF` (drop the path and start the `+0x640` timer, or `CellClass::Find_Blocking_Object 0x0047C5A0` → `Is_Ally_ByObject 0x004F9A90` → owner vtable `+0x1F4(1, object)`, else for a `Wall=` overlay `+0x1F4(1, cell)`: an Attack override on the wall cell); Walk/Hover carry the same Override arm (`0x00515C9C` wall case), and so does Ship (`0x006A319B` -> `0x006A31AA` -> `0x006A3238`). Corrected 2026-09-16 after a fourth independent review: the Override vtable slot has exactly ten `CALL [reg+0x1F4]` sites program-wide, of which Drive owns **one** (`0x004B3BE9`) and Ship one (`0x006A3238`) — each serving two logical arms, because the object arm ends `0x004B3B92 JMP 0x004B3BE3` into the very call the wall path falls through to. An in-tree correction that claimed "Drive owns two Override sites" was wrong and broke the ten-site arithmetic in the sentence above it; `0x004B3B94` opens the cell path rather than holding the call. That review also demoted the same-tick reach of the arm to **UNCHECKED** (the `arg2 = 0` recursion at `0x004B4552` is not shown to re-reach dispatch, and Drive makes no CALL between `0x004B3ADB` and it), and noted that commit a9525679 bundled an API change (dropping `MoverSearchFacts: Default`) under a prose-only title. Added 2026-09-16 (disassembly + decompile): the weapon route reads **weapon slot 0 unconditionally** — `PUSH 0x0` into vtable `+0x3F8` (`TechnoClass::GetWeapon 0x0070E140`, elite-only tier) at `0x0073F497`, then `+0xAC` — **not** the turret-aware current weapon that `Is_Armed` (`+0x2AC`) itself resolves, so the two gates read different slots; an unarmed mover leaves through the shared epilogue `0x0073FCD0` (branch `JZ` at `0x0073F48F`) while a warhead miss returns 7 at its own exit `0x0073F4C9`; and the infantry arm's warhead test `FUN_00772AC0` is **one** test of `+0x144` (`Wall=`) with no `+0x147` and no `Armor == 6` compare, so **`Wood=` is Unit-only**. Landed so far (99ce3ef5, 038aadfd + correction pass), all **inert**: `cell_entry::{WallArmContext, WallSearchCostClassifier}` (the producer), `authority::override_mission_on_wall_cell` (cell-target Override, still no caller, and carrying `#[allow(dead_code)]` with a pointer to this row until the crossing's wall arm lands), and `PathfindingContext::wall_cost` threaded through both A* families (three `_detailed` entries, 16 core forwards, 5 core entries → `AStarOptions::search_cost_classifier`); the four marker wrappers are test-only and pass `None`. `astar_search` consults the classifier **only on a refusal**: `neighbor_passable` carries the ground/bridge layer split and the `neighbor_cell.transition` (`0x200`) gate that a cell-scoped classifier cannot see, so replacing its verdict would silently drop them — the classifier is asked only "is this refusal a wall this mover may shoot?". Two defects found by an independent critic and fixed with regression tests: `classify` mapped `Clear => 0`, which could re-admit a refused ground→bridge neighbour at 1x (now `Clear => 7`, `the_wall_classifier_never_upgrades_a_refusal_to_passable`), and `weapon_route_code` applied the `Wood=` clause to infantry (now gated on `!is_infantry`, `the_wall_arm_wood_route_is_unit_only`) — stock-reachable via `[SHK]` (`Primary=ElectricBolt` → `[Shock]`, `Wood=yes` with no `Wall=`) against `[CAKRMW]` (`Armor=wood`, `Crushable=no`). A second independent review then found a third: native's wall arm does **not** return — it accumulates 4/5, falls through the occupant walk and reads the ground land row at `0x0073FAB5` (`FLD [ECX*4 + 0x89EA40]`, `FCOMP 0.0`), and a zero row returns 7 at `0x0073FAD0` (Infantry `0x0051C7D0`) whatever the arm accumulated, so the wall classes survive only where the terrain's own speed row admits the mover. `evaluate_shared_cell_leaf` now takes a separate `land_row_passable` = `speed_type_allows_cell` **alone**; the wider `land_passable` folds in `grid_ok` and the cost grid, and both go false on `overlay_blocks`, which `resolved_terrain.rs` sets for every `zone_class::WALL` overlay and `terrain_cost.rs` repeats — so gating on either (as first suggested) would have refused every wall and left the arm permanently dead. Pinned by `the_wall_arm_answers_seven_where_the_land_row_refuses`, which is also the first coverage of the overlay-backed producer path (`overlay_at` → `weapon_route_code` over a live `OverlayGrid` + `OverlayTypeRegistry`). Two honest limits on that gate, both raised by the third review. First, it is **stock-inert**: no `Wall=yes` overlay declares `Land=`, so `OverlayTypeFlags::land` falls back to `LandType::Clear` (`overlay_types.rs`) and `apply_overlay_land` writes that row, never the all-zero `[Wall]` row — so in stock data the row always admits and the gate never fires; it bites only where an overlay declares a land whose row is zero for the mover's SpeedType. Second, the first justification given for the gate was wrong even though the gate is right: it claimed the row is untouched by overlays, when `apply_overlay_land` sets `cell.speed_costs` from the overlay's `Land=` row. The conclusion survives for a better reason — native does the same, `CellClass::RecalcAttributes 0x0047D2B0` opening `this->LandType = ot->Land` (`+0x298`) with the early return on `Land == 4`/`9` or `NoUseTileLandType` (`+0x2AC`) that `uses_early_recalc_land_branch` ports — so the post-overlay row is the faithful analogue. The prose is corrected; the code is unchanged. The test hand-sets `speed_costs` rather than driving the production overlay-land path, so that path is still uncovered. That inertness is native-matching, not a VERA quirk: `OverlayTypeClass::ReadINI 0x005FE770` reads every field in the echo form `ReadX(section, key, *(this + off))`, so an absent key keeps the constructor default, and its field map records `+0x298 Land` defaulting to `0 = Clear` with `+0x2AC NoUseTileLandType` defaulting to true — the same pair VERA uses. The body corroborates the `Land` default independently of the annotation: the forcing rule `if (Tiberium && Land == 0) Land = 5` is only coherent when the default is 0, and VERA ports it verbatim. So a stock wall carries the passable `[Clear]` row in gamemd too, its own 4/5 codes clear the land row, and the wall-attack Override is reachable — which a `Land` default of 4 would have made impossible. The constructor itself was not read; the evidence is ReadINI's echo pattern plus the Tiberium forcing rule. Recorded rather than fixed: VERA composes the wall class and the `entity_block_map` code **multiplicatively** (`core.rs` applies `apply_search_cost_class_multiplier`, then `step_cost *= mult`), where native takes a running max over codes and applies one `0x0081870C` entry — a wall cell that also carried a block-map entry would cost 60x*20x instead of the native 20x; reachability is very narrow, since a wall cell rarely holds an occupant. A first attempt cached the mover's `is_armed`/`Wall=`/`Wood=` facts on `MoverSnapshot`; that was reverted because `snapshot_mover` runs per mover per tick and `rules.object()` allocates a `String` through `type_handle`, i.e. ~20k allocations/tick at the A10 target for flags nothing read — they belong at the search site, resolved only when a path request is built. They cannot be cached on `GameEntity` beside `CrushCapability`'s flags: `primary_for_tier` is veterancy-dependent and promotion never refreshes cached type flags. Remaining: nothing constructs a `WallSearchCostClassifier`, so the producer is still unexercised in production; the runtime `CellEntryResult::FriendlyWall` producer needs a result-preserving terrain entry (both runtime gates call `is_cell_passable_for_category_on_layer`, a `bool` wrapper that flattens `WallBlocked` into `Impassable`); the joint arms at `movement_occupancy.rs` and `movement_tick.rs` still fold `FriendlyWall` into `Impassable` instead of running the cell-target Override; and coverage must reach all ~18 production callers of `issue_move_command_with_layered` or it rebuilds the I9c per-caller divergence (only `world_commands`/`world_orders` hold the overlay+alliance tables; `scatter`, `miner_*`, `production_queue`, `transport_unload`, `bump_crush` have no overlay access). The research notes recommended one request struct over more positional flags; the seam first threaded a 20th positional argument instead, and the third review objected that wiring the producer would then have to re-touch the same sites. So the refactor landed before the producer, not after: `MoverSearchFacts { urgency, mover_is_crusher, is_infantry, wall_cost }` now carries the four facts the A* cost evaluation consults per neighbour, across **eight signatures and twenty-seven call sites** (measured; an earlier draft of this row said "seven entries and twenty-two forwards", undercounting `find_path_zoned_marker_inner_detailed` and its two forwards). The struct is deliberately not `Default`: a defaulted value means `mover_is_crusher: false`, which is precisely the I9c regression shape recorded above, so every construction site names every field. Recorded follow-up, raised by the increment's critic: the regroup stops one layer short — `AStarOptions` still holds `urgency`, `mover_is_crusher`, `is_infantry` and `search_cost_classifier` as four separate fields, unpacked at five sites, so the *next* mover fact still costs a field there; folding `MoverSearchFacts` into `AStarOptions` would finish the job. Also verified by that critic: the reordered signatures are safe for a stronger reason than line anchoring — the parameter *type* changed, so any stale positional caller fails to compile rather than silently swapping adjacent bools. `find_path_zoned_marker_detailed` drops 18 → 15 parameters and `find_layered_path_zoned_marker_detailed` 20 → 17, for a net −34 lines, with the suite unchanged at 9000/0 (a pure regrouping must not move it). Deliberately excluded, because they are different things: `allow_zone_hierarchy` and `playfield_bounds` are hierarchy-admission inputs rather than per-neighbour cost facts, and the two movement-zone parameters are distinct — `mz` selects which zone map/hierarchy to consult (`zg.map_for(mz)`, `zg.hierarchy_for(mz)`, `zg.can_reach(mz, …)`) while `movement_zone` is the mover's own zone for passability, with `movement_zone.unwrap_or(mz)` the documented fallback; collapsing those two would have been a silent bug. The producer's next mover fact is now a struct field rather than another edit across seven signatures. How native drops a cell TarCom once the wall segment dies is UNCHECKED (`FootClass::Mission_Attack 0x004D4DC0` read; no cell-validity site found yet), though VERA's `expire_cell_target_references` already clears and Restores on wall death. Research notes: [WALL_ARM_CAN_ENTER_CELL_NOTES.md](../research/WALL_ARM_CAN_ENTER_CELL_NOTES.md). **Runtime arm LANDED 2026-09-16** (6195c76a, e624fb2c, 85c88455, 4d16b7aa, fba12a8a): a mover refused by a wall it is armed against now attacks it instead of repathing forever. Mechanism: `WallArmTables` (overlay grid, overlay registry, alliance map) rides on `PathfindingContext`, populated at the three production ctx builders, so none of the ~17 order-time call sites gains a parameter and none can diverge - the property I9c lacked. The mover facts (`is_armed`, slot-0 `Wall=`/`Wood=`) resolve on `MoverSnapshot` through `TypeHandleTable::handle_for` -> `object_by_handle`, **zero heap allocations per mover per tick**, which is the hazard that forced `2b877fec` to revert `038aadfd`. `evaluate_cell_entry_for_category_on_layer` is a result-preserving sibling of the bool predicate, and the bool predicate is now implemented in terms of it, so "identical when no wall context is supplied" is structural rather than a maintained claim. The Ground crossing arm keeps the verdict; the refusal block splits three ways (ordinary block verbatim and clears the memo; first wall refusal records the cell and repaths as any block does; repeat at the same cell defers the Override and breaks **without** `handle_blocked_tick`, which would scatter and re-arm the repath loop this exists to break). The Override is dispatched outside the entity borrow and pushes the mover onto `finished_entities`, which is the once-per-block guarantee: a second Override with an empty queue archives the CURRENT mission, so a re-entering mover would overwrite its archived Move with Attack and every later Restore would return it to Attack. **Native reach, narrowed not closed:** the drop branch IS the producer of the retry - `0x004B3ADB` writes `Foot+0x5E0 = -1` and stamps `+0x640 = Frame` with duration 0, `0x004B3AFE JMP 0x004B4541` lands three instructions before `CALL 0x004B2630` at `0x004B4552` with `arg2 = 0` (`PUSH 0x0` at `0x004B4546`), and `0x004B283F JGE` reads elapsed 0 against duration 0 and proceeds; `0x004B3650` is the sole jump into the dispatch program-wide. What is **not** traced is the re-entry gate chain: with `Path[0] = -1`, `0x004B2652 JNZ 0x004B26D0` is taken only if `CALL [ECX+0x10]` (Is_Moving) at `0x004B264D` answers true, and `0x004B2813` is reachable only from `0x004B2792`/`0x004B279C`/`0x004B27FA` inside that branch. The 4/5 arm writes neither Destination nor those gates, so true is expected - argued, not executed. An earlier commit message claimed the disassembly "closes it"; it does not. **DRIFT:** native's two evaluations share one `Process_Movement` call; VERA's second refusal lands a tick or two later, so `MovementTarget::wall_refusal_cell` spans the gap. It is preserved across the segment repath at `movement_tick.rs` (the one production site that rebuilds a target mid-order - resetting it there would mean the Override could never fire) and cleared as soon as a different cell is attempted. **OPEN, and it bounds what landed:** `WallSearchCostClassifier` still has no production construction site, so A* hard-blocks walls where native prices them 60x/20x and routes through. Consequence, found by the increment's critic: the arm fires **only on a fully sealed wall line** - if any detour exists the repath takes it and the memo clears. Native attacks whenever the detour beats 20x, the common case against a player wall. Wiring the classifier needs `Option<WallArmContext>` at `find_move_path_with_marker_detailed` and its ~6 production callers, and the callers lacking an interner or mover facts would pass `None`, which is the I9c shape - so it is a design step, not a mechanical thread. **Also open:** no test reaches the arm, because the fixture harness `tick_movement_with_grid` carries no `RuleSet` and the wall facts resolve through the handle table, so `is_armed` is false for every mover in every existing test; the suite's 9000/0 shows only that nothing regressed. `wall_refusal_cell` is absent from the world hash - it is deterministically derived and its effects surface through mission state, which is hashed, so this is detection granularity rather than a desync, but it needs a `HashFeature` bump and a pinned-constant rebaseline. **Corrections from PR #387's independent critic, all confirmed against the tree or the binary.** (a) **Reachability is narrower than a sealed wall line.** Order-time A* hard-blocks walls and `resolve_reachable_move_goal` (`movement_path.rs:309-345`) retargets an unreachable goal into the mover's own zone, so a move ordered against a wall that is **already standing** never puts a wall cell in the path and the arm cannot fire at all. It fires only where a wall appears across a **live** path and the following repath then fails outright. So the case this actually fixes is a unit walled in mid-move; ordering a tank past a standing enemy wall is untouched, and that is the commoner case. (b) **The untraced gate cuts the other way.** If `Is_Moving` at `0x004B264D` answers **false**, native goes `0x004B2659` to `0x004B2698 CALL [EDX+0x184]`, compares 2 and returns without repathing - the Override never fires. VERA's port assumes the true answer, so the untested risk is **attacking where retail does nothing**, not merely missing an attack. (c) **The A* channel was removed here, not merely left unwired.** This increment renamed the `PathfindingContext` field `wall_cost` to `wall_tables`, so the two production forwards at `movement_path.rs:522` and `:601` became a literal `None`. It is behaviourally inert - `wall_cost: Some(..)` appears nowhere on `origin/main` either, so the channel never carried anything - but the PR body said the classifier had no production construction site without saying its context channel had just been folded away. Wiring A* will now build the classifier **from** `wall_tables`, which is the better seam, but that is a future step. (d) `walk_path.rs:247-251` builds `wall_tables: Some(..)` for a context whose only consumer discards them, so one of the three builders is currently inert. (e) **Timing against the stuck abort is unpinned:** the failed-repath branches go through `start_movement(.., path_delay_ticks, ..)` and nothing establishes that the second refusal lands before `PATH_STUCK_INIT = 10` (`mod.rs:256`) runs out. (f) The once-per-block comment at `movement_tick.rs:3178-3182` explains the wrong mechanism: what prevents re-entry is `finalize_finished_entities` clearing `movement_target` (`:4452`); the double-archive story is the hazard being avoided, not the guard. (g) `cell_entry.rs:58-68` still reads fully pre-I9b. (h) The examination reports are now tracked in `docs/research/` as `MOVEMENT_ACCEPTANCE_A2/A3/A5/A6/A9_*_EXAMINATION.md`, so the detail behind these rows is reproducible by a reviewer; they were session-local when the critic raised this. **Trigger and frequency, recorded plainly because the reach is narrow.** **Trigger:** a wall must appear across an **already-moving** mover's path *and* the repath that follows must fail outright. A wall standing when the order is issued never triggers it, because order-time A* hard-blocks wall cells and `resolve_reachable_move_goal` retargets the goal instead. **Frequency: rare**, being the conjunction of those two - a unit walled in mid-move. The common case, ordering a tank past a standing enemy wall, does not trigger it at all, and closing that needs the A* pricing this row leaves open. |
| I4b Hover hard-turn translation while braking | A9 | OPEN (divergence established 2026-09-16) | **Native (verified from the binary, not from notes):** `HoverLocomotionClass::SpeedUpdate` reaches the stall through **three** gates, not one - an earlier version of this row named only the middle one and was corrected by PR #387's critic. (1) `0x00515F99 CALL [ECX+0x60]` on the owner; `0x00515FA3 JZ 0x00515FD1` sends a **false** answer onward with **no turn condition at all**. (2) Otherwise `0x00515FA5..0x00515FCF` takes the absolute facing difference (`SUB AX`, then `CDQ/XOR/SUB`) and `CMP EAX,ECX` against `0x2000` with `JGE 0x00515FDC`, so only a turn **strictly wider than 0x2000 (45 degrees)** continues - exactly 45 does not. (3) Both arms land on `0x00515FD1 MOV AL,[ESI+0x70] / TEST AL,AL / JZ 0x00516103`: the stall is the zeroing at **`0x00516103`** and runs only when loco `+0x70` is zero; a non-zero `+0x70` falls through to `0x00515FDC` and does not stall even on a wide turn. The stall sets **`SpeedRequest = 0` and nothing else**; `SpeedCurrent` (loco `+0x50`) then merely decays at `HoverBrake` (`0x005161C9..0x005161E4`). `Move` re-derives `ftol(base Speed x SpeedCurrent)` at `0x00514372..0x00514383`, and the XY step is admitted at `0x00514746..0x0051474C` on `TEST EAX,EAX / JLE` - **a positive budget alone, with no turn gate anywhere**. Retail hover therefore keeps translating through the corner while it brakes. **VERA:** hard-stops instead - `movement_tick.rs:2437-2440` zeroes both `effective_speed` and the frame budget, and `hover_mover_swings_through_corner_braking_not_freezing` (`movement_tests.rs:5788`) pins "no lepton drift while stalled", so the divergence is currently locked in by a test whose name reads the opposite way. **Trigger: NOT established** - it depends on the `+0x60` predicate and on loco `+0x70`, neither of which is identified (`+0x70` has no located writer; see the UNCHECKED list in I7). What is established is the VERA side and the shape of the native one. The widest the trigger could be is any corner wider than 45 degrees taken while already moving - a 90-degree route corner, a re-order mid-move, an attack-move re-aim. Exactly 45 degrees does not trigger it, and a turn from rest does not diverge. **Frequency: an upper bound, not a measurement** - it is whatever fraction of wide corners also satisfies the two unidentified gates. The bound is high, and on a mainline unit: `[ROBO]` Robot Tank is TechLevel 2 Allied, plus the three naval transports `[LCRF]`, `[SAPC]`, `[YHVR]` (the four stock `Locomotor={4A582742-...}` users; the fifth CLSID hit is commented out under `[YURIPR]`). At ROT=5 the stall lasts about 6 ticks for 90 degrees and about 19 for 180. **Those two are consistent and must not be "corrected" to 12 and 25:** the stall runs while the delta exceeds `0x2000`, so it ends when the delta *reaches* `0x2000`, giving `(0x4000-0x2000)/1280 = 6` and `(0x8000-0x2000)/1280 = 19`. The ratio is 3 rather than 2 because of that offset. 12 and 25 are the **whole-turn** `Is_Rotating` window (`abs(diff) / (ROT << 8)` at `facing_class.rs:141`), which is a different quantity - a reviewer applied it here and read the pair as contradictory. **Player-visible effect:** retail sweeps a continuous arc through the corner; VERA stops, rotates in place, then resumes - a hover unit moving like a tracked one. |
| I5 Path smoothing rejection terms | A2 | OPEN (scope corrected 2026-09-16) | `path_smooth.rs:18` ports `0x0042B420` without its three rejection terms (`0x0042B5A5..0x0042B5F5`). The A2 examination found the row understates the gap in two ways. First, native's post-processing is **two passes**, and this row covers only the first: the second, `0x0042B7F0`, is a straight-segment optimiser and loop detector, and VERA's stand-in `find_drift_segment` (`path_smooth.rs:486`) is **inert** - the tree's own `path_smooth_tests.rs:251` already says `optimize_path` is dead and that it can never fire. Second, a finding the prior research did not record: **T3 samples the wrong cell.** The per-step z and the bridge lift are also described in the report. Splitting pass 2 into its own row is the open question this increment should settle first. Report: [A2 examination](../research/MOVEMENT_ACCEPTANCE_A2_PATH_SMOOTHING_EXAMINATION.md). **Trigger.** Pass 1's three missing rejection terms (`0x0042B5A5..0x0042B5F5`) trigger on **every path A* smooths**, which is every multi-cell move order. Pass 2 is worse than intermittent: `find_drift_segment` is **inert**, so `0x0042B7F0`'s straight-segment optimiser and loop detector never run at all - the trigger is every path, and the miss is total rather than partial. **Frequency: STRUCTURAL and continuous**, on the only routing path there is. |
| I6 Scatter model | A5 | OPEN | `scatter.rs` header retraction; `Scatter_Objects 0x00481670`. |
| I7 Hover locomotor phases 2/3 | A9 | STALE ROW - scope already delivered (2026-09-16) | The row named phases 2 and 3 as outstanding; both are in production. Phase 2 is `hover_steer` (`movement_step.rs:316`) with the throttle at `movement_tick.rs:2312-2373`; phase 3 is `hover_vertical_tick` (`movement_tick.rs:4247-4311`), consumed by the renderer at `render/locomotor_visual.rs:136`. Hover no longer rides the Drive track. Matching mechanisms carry native addresses: the `0x2000` turn threshold, the `ftol` step derivation, the 0xFF/0x100 proximity arm, the boost and its post-boost clamp, the `1/(rate x 900)` ramps and `FUN_00513D20` for the vertical. What is NOT delivered is tracked separately and should not be folded back into this row: the hard-turn translation divergence is I4b, and a newly found one needs its own row - `SpeedUpdate 0x005161E7..0x005162B9` is the last facing write of every cruising tick and aims at **Head_To plus one cell along the queued next direction** (`0x00516211`/`0x00516230`/`0x00516241`), applied through rate-limited `FacingClass::Set 0x004C9220` on owner `+0x388`, whereas VERA always aims at the current waypoint. Trigger: every cruising tick with a queued next step; frequency: continuous while a hover unit follows a multi-step path; effect: retail leads the corner, VERA aims at the corner. UNCHECKED, with instruments: the loco `+0x70` gate read at `0x00515FD1` has no located writer (needs a writer sweep plus a breakpoint there); the loco `+0x30` versus owner `+0x388` two-facing split (needs the renderer's facing read or a retail `-win` capture); the turn-decay tail at `Move 0x00514A21..0x00514B60`; and the deep-water/bridge arms at `0x00514B60..0x00514C12`. Doc corrections owed: `hover.rs:14-16` is stale, `movement_step.rs:312` cites a plan document that does not exist, and the hover research report's "`Set` = snap" is wrong, omits the look-ahead tail, and its "slot 0xEC = force-float-up" conflicts with `ObjectClass::DropIn 0x005F4160`. |
| I12a Jumpjet cruise through the native Update and translate bodies | A11 | LANDED (PR #381) | Before: a cruising Jumpjet used the Fly adapter (8-bit facing at `ROT=`, a 3/2/0.5-cell speed-fraction table, a halving brake inside 86 leptons, arrival within 128, fixed height). Native established (2026-09-15/16, [JUMPJET_FLIGHT_NATIVE_NOTES.md](../research/JUMPJET_FLIGHT_NATIVE_NOTES.md)): `Update 0x0054D0F0` speed ramp with the accelerate-then-brake overshoot, bob, height integration over the `0x0054D820` look-ahead reference (cell top `0x00485080`, bridge deck quirk), low-altitude speed gate, XY step along the locomotor facing; `State3 0x0054BFF0` atan facing, four zones with an asymmetric turn-error slowdown, 3/4 and 1/2 height dips, arrival into state 4 or a held hover; `Link_To_Object 0x0054AD30` sets the facing rate from the type `JumpjetTurnRate=`. The process x87 word is `0x0E7F` (53-bit, chop), so the kernel uses `X87Chop53`. Parity demonstrated: `tools/spatial_oracle/jumpjet_flight.json` (14 native rows: cruise, reverse, left and right turns, low start, balloon and target holds, no-wobble diagonal, a sloped look-ahead cell, a west-facing start, the Siege Chopper `IsSimpleDeployer=`+`DeployToLand=` hold, a water destination, a piggyback arrival and the `JumpjetHeight=` floor; bridges, building tops and cell objects outside the corpus) matched frame for frame, body facing included, by `flight_matches_the_native_update_and_translate_corpus`, and flown through the production air tick by `production_cruise_flies_the_native_east_cruise_frames` and `production_cruise_turns_from_the_body_facing` (red with the cruise branch disabled). `g_HeightFactor` = 104 is read from the native startup chain (`tools/spatial_oracle/height_factor.json`, `building_height_factor_matches_the_native_startup_value`). The adapter's takeoff stands in for State 0, so cruise entry applies its seed (body facing, zero speeds and bob, full height). Rust regression tested: bridge-ahead deck quirk, cell ahead max/average, building over object lift, turn-error asymmetry, atan2 branches and table bytes. Rust: `movement::jumpjet_flight`, `world::jumpjet_cruise`, `retail_trig::AtanTable`, `ObjectType::{is_simple_deployer, deploy_to_land}`, binary32 `JumpjetParams::{climb, crash, accel}`, `JumpjetRuntime::{params, flight}` (snapshot v163). Residuals (owned by I12b): a cruise whose move target drops stops and holds (`an_interrupted_cruise_holds_with_no_speed`) where native `Stop_Moving 0x0054B4D0` re-targets a nearby cell and keeps flying; the unit destination is the goal cell centre, not the `Move_To` FNPC/`0x0054D6D0` selection, which differs only when the ordered cell is not passable; takeoff still climbs without the native state-1 translation, idle hold has no bob and a non-balloon hold with a target descends at once, descent uses the adapter crash-speed rate and skips the landing admission of `0x0054C550`; the cell `AltObject` air slot (`0x004135A0`, `0x00487D70`) is absent, so jumpjets ordered to one cell stack instead of scattering to a neighbour (trigger: a group move to one cell; frequency: common for Rocketeers and Kirovs); `JumpJetTurn=` (LUNR) hold facing, the planning-token arrival notify `0x00705D60` (inert without `TechnoClass+0x514`), the piggyback arrival call and the grounded reset vtable `+0xF4` are unmodelled. |
| I12b Jumpjet states, the Process gate and the cell air slot | A11 | LANDED | Before: VERA's air adapter owned every Jumpjet frame, so an idle non-balloon one cycled Ascending f0 -> Hovering f50 -> Descending f51 -> Landed f101 -> Ascending f102 forever (confirmed by probe 2026-09-16). Native established (disassembly 2026-09-16, [JUMPJET_FLIGHT_NATIVE_NOTES.md](../research/JUMPJET_FLIGHT_NATIVE_NOTES.md)): `Process 0x0054AEC0` runs `Update 0x0054D0F0` only while the moving byte is set or `Is_Moving_Now 0x0054D0D0` (state neither 0 nor 2) answers true, then dispatches the jump table at `0x0054B19C`. State 0 `0x0054B980` snaps the locomotor facing to the body facing, zeroes both speeds, seeds `+0x80 = JumpjetHeight=` and promotes to 1. State 1 `0x0054BA30` releases horizontal speed above `+0x80/4` (`/2` for a hovering Unit), and at cruise height claims the cell air slot or scatters to a `RandomRanged(0,7)` neighbour. State 2 `0x0054BD30` re-opens the cruise on a moved destination, holds for a TarCom, a `BalloonHover=` type or a `DeployToLand=` deployer, else releases its slot and descends - each through the arrival notify `0x00705D60`, NOT `0x006385C0` as the decompiler's thunk name suggested. State 4 `0x0054C550` turns a Water/Beach landing into a hold, admits through `Can_Enter_Cell` and `IsSubCellFree` with a Unit on sub-cell 0 always admitted, hands a refusal to `Stop_Moving`, and touches down into state 0. `Move_To 0x0054B1C0` restores `+0x80 = JumpjetHeight=` when it lifts a descent back to state 1. Two distinct direction tables, both BSS: `0x0049F3A0` fills the 8-byte lepton deltas at `0x0089F6D8` (reference height `0x0054D820`), `0x0049F2F0` fills the 4-byte packed adjacent-cell offsets at `0x0089F688` that `0x00481810` steps by; the cached cell `+0x560` is written only through `+0x2F8` (`0x0041C160`), never by Mark (`0x004D3780`). Parity demonstrated: `tools/spatial_oracle/jumpjet_states.json` - 10 rows driving the real `Process` (idle landed, idle hold, takeoff-cruise-land, balloon hold, in-cell translate, scatter from a taken slot, the water hold/descend alternation, a blocked landing re-targeting a free cell, a hold re-order, a sloped takeoff) - matched frame for frame by `states_match_the_native_process_corpus`, which compares the kinematics AND the native air-slot call sequence (`slot_events`: query, claim, release) frame by frame; the `RandomRanged` draw and the FNPC re-target cell are supplied inputs as the corpus declares. A first pass compared kinematics only and hid a slot leak (below). Rust regression tested: `an_idle_landed_jumpjet_never_leaves_the_ground`, `an_idle_hold_without_a_move_is_inert`, and the two I12a cruise rows re-flown through the real tick. Rust: `movement::jumpjet_flight::{process, state0_ground, state1_ascend, state2_hold, state4_descend}`, `world::jumpjet_cruise` (owns every Jumpjet tick; `AirMovePhase` and altitude derived from the state byte), `occupancy::AirSlotGrid` + `ObjectSubstrate::air_slots` (snapshot v164, `fold_air_slots`, released on entity teardown), `JumpjetRuntime::landing_latched`; `air_movement::tick_air_movement` now excludes Jumpjets outright. Residuals: `Stop_Moving 0x0054B4D0` has no FNPC search here, so a refused landing stays in the descent and re-tests admission every frame instead of re-targeting a nearby cell (answering the hold instead would strand it, since the Process gate would then advance nothing); `Move_To`'s FNPC relocation of an impassable ordered cell is unmodelled (trigger: an order onto a blocked cell; effect: the owner holds where native would shuffle to a neighbour); `Can_Enter_Cell`'s graded 0/2/>2 answer collapses to clear or refused; State 5 crash `0x0054CA90` and the `+0x425` latch, `RulesClass+0x48`'s deploy facing, owner `+0x134`, `JumpJetTurn=`, the air buckets and State 0's `0x00ABC588`-gated bucket add are unported; `Process`'s owner `+0x90` dispatch gate (`0x0054AEF5`, an object alive/valid flag whose clear state skips the entire state dispatch) and the `+0xF0` landing callback are unported; VERA maintains no cached cell `+0x560`, so State 2's cached-cell slot release (`0x0054BF75..0x0054BFA8`) is modelled as dropping every slot the owner still holds - without it a claim orphans when the owner drifts out of the cell it claimed, leaking into the hashed, snapshotted grid (the corpus cannot witness that branch, since its declared substitution pins `+0x560` so the original skips the cached-cell release; covered instead by `re_opening_a_cruise_drops_a_drifted_slot`); the superseded non-native helpers in `movement::jumpjet_movement` (`tick_jumpjet_altitude`, `tick_jumpjet_acceleration`, `apply_turn_rate`, `compute_wobble`, `should_land`) are now unreachable and await removal; bridges, building tops and cell objects in the reference height stay Rust-tested only. **Frequency.** This row carries no player-visible divergence to rate: what it lists is a **coverage limit** (bridges, building tops and cell objects in the reference height are Rust-tested only) and **dead code awaiting removal** (the superseded `jumpjet_movement` helpers, now unreachable). The jumpjet divergences that do have a frequency are in I12a. |
| I13 Trace-corpus pace audit | A12 | RECORDED (documentation only) | The audit that produced A12. Six movement defects this programme's own traces report as live were re-checked against current code and are **all stale**, each invalidated by `e163a3aa` (2026-07-30) and never revalidated; `5aaa9c22` (2026-08-18) first tracked the corpus wholesale, importing 2,544 files without review, which is why the claims persisted. Verified stale, each from source: the ~3× wall-clock pace gap (E2 row 15, AMCV_REVERSE_180 row 6 — both derive from `speed × 22/1000`, and nothing in production multiplies by the 22 ms tick); the fresh-track cursor starting at `entry_index` rather than 0 (`begin_selected_drive_track` ends `state.point_index = 0`, the chain path uses `entry_index - 1`, and `drive_track.rs:3883` documents the split); the accel/brake ramp running 3× fast (its cited gate `drive_track_native_frame_count` and `DRIVE_TRACK_NATIVE_FRAME_HZ` have zero occurrences in `src/`, and the sole production call of `update_drive_speed_fraction` is `movement_tick.rs:2221`, once per admitted frame — the five others are inside `mod tests`); ROT turn duration 2× fast (`test_body_rotation_matches_native_frame_duration` pins ROT=5 at 12 frames for 90° and 25 for 180°, its comment naming the old ms-integrated path as the ~2× defect); movement timers decremented per 45 Hz tick (`CdTimer` is frame-anchored — `remaining = current_frame - start_frame` by wrapping subtraction, with no decrement at all, read via `expired(native_frame)`); and the debug 3× MCV speed multiplier (`world_commands.rs:73` is now a `MoveInfo` field, and no speed multiplier exists anywhere in `sim/`). Carried, not fixed: the corpus still asserts the dead premise. A pattern sweep of `docs/research/traces/` (`45 ?Hz\|45Hz\|SIM_TICK_HZ ?= ?45\|22 ?ms\|22ms\|sub-step\|3 sim ticks\|one frame per three\|15 ?Hz gate\|binary_frame ?= ?\(total_sim_ms`) matches 19 of 204 documents; a separate sweep for citations of the three files `e163a3aa` deleted (`app_sim_tick.rs`, `app_types.rs`, `app_context_order.rs`) matches 32, of which 25 carry no pace claim — so the two debts are distinct and must not be merged. Both counts are **method-bounded upper bounds, not a census**: known false positives include the two `MAIN_MENU_*` traces (UI paint cadence), `MISSION_HARVEST` (`HarvesterLoadRate * 3`) and an ore-growth cadence row, while `POWER_BAR_FLASH_PHASE` and `POWER_BAR_SEGMENT_SLIDE` are genuine *mechanism* findings (gamemd reads `GetRadarTimer() = timeGetTime() >> 4` where VERA counts game ticks) that survive the premise change and must not be swept with it. An earlier regex-only pass of this same sweep missed four documents and over-matched three, so the remaining work is per-document re-validation, not a bulk edit. Root document corrected and logged in [AUDIT_LOG.md](../research/AUDIT_LOG.md) 2026-09-16. |
| I14 Drive residual budget: fresh-curve cursor, terminal credit and the headless reset | A3, A12 | **D1 and D2 LANDED 2026-09-16**; D10 open | The three A3 divergences that share one mechanism - how much budget a Drive mover spends to cross one cell. **D1 and D2 landed together** because correcting either alone leaves the per-cell cost wrong in the other direction. D1: native pays before it reads (`0x004B159D SUB EDI,0x7` then `0x004B1596`) and increments only at the tail (`0x004B1F4F`), so a fresh cursor of 0 (`0x004B4659`) occupies `points[0]`; the port incremented first, so `points[0]` was never occupied. The site was `track_head::begin_fresh`, and VERA's unsigned cursor cannot spell native's pre-start -1, so `DriveTrackState::before_first_point` is that state. Chained adoption was already correct **for D1** and is untouched by it. D2 is a different matter and the first version of this row got it wrong: the old loop ended on `point_index >= last_index`, so a **chained** curve never paid for the sentinel read or took the credit either. Its per-cell cost moves by the same `14 - credit`, and no chained-curve budget test exists. D2: the curve ends on the `(0, 0)` sentinel one slot past the real points (`0x004B15C0..0x004B15C8`), that read costs its 7, and `0x004B1FD0..0x004B1FF9` credits `ftol((1 - manhattan/11) * 7)` back (`0x007E7FB0` = 7.0, `0x007E7FB8` = 1/11, `0x007E1718` = 1.0, all read from the binary). A negative term is normal, though an earlier version of this row explained it with the units the wrong way round. 7 is what a point **costs** (`SUB EDI,0x7`); 11 is what a point **spans** - track 1's y runs 245, 234, 223 ... 3, exactly 11 leptons a step, and the diagonals step 8 and 8 for a manhattan of 16. So the credit says a whole step was paid for but only `manhattan` leptons of it were really left, and a curve ending further out than 11 is charged for ground the final snap still covers - track 1 ends at (0, 3) and credits +5, track 15 ends at (16, -4) and credits -5. Raw track 1 now costs 161 + 7 - 5 = **163** per cell where the port spent 154. The correction is `14 - credit`, so it is **per track, not one figure**: track 1 (straight) 154 to 163, +5.8 percent; **track 2 (diagonal) 210 to 227, +8.1 percent**; track 3 371 to 382, +3.0 percent; track 13 469 to 477, +1.7 percent. Straight and diagonal are the two common cases and they bracket **6 to 8 percent**, so the single "about 5 percent" this row first carried was a track-1 number. Pinned by `fresh_curve_occupies_point_zero_and_a_cell_costs_the_native_budget`, whose numbers are derived from the native loop rather than read off the port. **D10 still OPEN, and narrower than the examination reported.** The report said native zeroes the residual on every headless or selectorless frame; the two jumps into `0x004B25F2` show it is an **early-exit precondition at the top of `Process_Drive_Track`**, not a per-frame reset. `0x004B0F2C` reads loco `+0x63`; a clear flag, or a set flag with selector `+0x58` == -1, falls to `0x004B0F3C CMP owner+0x5E0,8` and `0x004B0F43 JNZ 0x004B25F2` zeroes loco `+0x4C` and returns unless the owner is in state 8. Separately `0x004B0F49` tests loco `+0x62`, and when set, `CALL [vtable+0x84]` then `0x004B0F5B` reads byte `+0xCA1`; zero also lands on `0x004B25F2`. Trigger: a tick where the mover holds no usable track and is not in state 8 - which includes the in-place turn. Frequency: every in-place turn, so common. Effect: VERA carries up to 7 stale budget into the next curve where retail starts from zero, worth about 11 leptons once per turn. Blocked on identifying the VERA equivalents of loco `+0x62`/`+0x63`, the state-8 constant on `Foot+0x5E0`, and the `+0xCA1` byte behind vtable `+0x84`. **Residual carried by this increment:** `gsi_04_05_forced_refinery_exit_...` guarded against a path that zeroed the owner residual outright and could no longer be written as `!= 0`, because the forced refinery exit runs raw track 15 and the credit lands the arithmetic on zero legitimately. It is pinned instead, which is a **weaker guard**, and nothing else currently covers the original hazard; restoring it needs a fixture whose forced terminal ends with budget left over. **Evidence class:** native established and Rust regression tested; **not** parity demonstrated - no motion-over-time instrument exists, so the per-cell figures are arithmetic over the native loop and the read track data, not a capture. **Further corrections from this increment's critic, which verified every native claim in the change before finding these.** (i) **A snapshot blocker, fixed:** `before_first_point` went in mid-record and `GameEntity.drive_track` is bincode-serialized, so `serde(default)` was inert and a v164 save would have decoded `residual`, `transform_flags` and both head offsets from the wrong bytes rather than being rejected. `SNAPSHOT_VERSION` is now 165. (j) **The credit is unclamped, and thirteen of fifteen tracks were untested.** Fourteen land between +6 and -6; **track 11 ends at (96, 85), 181 manhattan out, for -108** - about fifteen ticks a mover could not afford a step. Latent today, because `0x47` is the only forced index wired and it selects track 15, and now pinned by `terminal_credit_is_pinned_for_every_shipped_track`. **UNCHECKED:** whether any production path runs track 11 to its end; if one is found, the premise to re-examine is that the head is where the sentinel maps, because a curve ending 181 leptons from its own head is the part that looks wrong. (k) **`finished` changed meaning** - from "reached the last real point" to "paid for the sentinel" - and its consumers were not surveyed: `movement_step.rs:1839`, `:1877`, `:1891`, `:1907` and `movement_tick.rs:153`, `:170` are untouched. A curve parked on its last point with residual at or under 7 now reports `finished == false` until more budget arrives, and `drive.track_valid = !advance.finished` keeps it alive across that gap. Native-faithful, unasserted at the consumer level. (l) **Native persists the residual under gates VERA does not model.** `[EBP+0x4C]` is written at only two addresses in the whole function: `0x004B1F64` (normal loop exit) and `0x004B25F2` (D10's zeroing). The terminal path reaches the first only via `0x004B22AF JNZ 0x004B1F5C`, gated on `[obj+0x90] != 0` and a zero return from `[vtable+0x504]`; `0x004B2277`, `0x004B2295` and `0x004B22B5` return **without** persisting. VERA persists unconditionally. Also unmodelled: native's terminal resets `[EBP+0x58] = -1` and `[EBP+0x5C] = 0` at `0x004B210E`/`0x004B2115`. (m) **The gsi_04_05 replacement value is a pin on observed output**, not derived the way the new test's numbers are, and its comment calls TurnTrack 71 "the forced refinery exit" while `drive_track.rs:201` says `0x47` is the **Tank Bunker eject, not a refinery** and `miner_tests.rs:4895` has a miner using it. The tree contradicts itself and this increment picked a side silently; settling it is open. **A second critic pass on the published PR found two more consequences of D1, both now fixed, and three records.** (n) **The occupation handoff gate had gone off by one.** Native compares its **stored cursor** against `RawTrack+0x0C` (`0x004B49B6 MOV EDX,[ESI+0x58]` / `CMP EDX,ECX` / `JGE` out), and `ESI` is the locomotor biased by -4 - confirmed by three offsets lining up: `ESI+0x54` indexes the TurnTrack table (the selector, `+0x58`) and `ESI+0x5C` is read as a byte (the variant, `+0x60`), so `ESI+0x58` is the cursor at `+0x5C`. That cursor counts points **occupied**, one ahead of `point_index`, so after D1 VERA held the forward handoff claim one paid point longer than retail on every curve that has one (tracks 3, 4, 5, 6). (o) **The paid-point block was skipped on a fresh curve's first step.** The gate compared `point_index` against its value before the advance, and a fresh curve occupies `points[0]` leaving the index at 0 - so for exactly the step D1 exists to fix, and whenever the tick afforded one point (budget 8 to 14, the ordinary case at 163 a cell), the impatience clear, the occupation clear, the node consume and the height commit were all skipped. Both are fixed by `DriveTrackState::occupied_points`, which is native's cursor, and `TrackProgress::cursor` now carries it - restoring the meaning that field's own doc comment already claimed. (p) **A third install convention - and calling it UNCHECKED was wrong; the binary settles it in one instruction, and it was a live bug.** `DriveLocomotionClass::Force_Track 0x004B0C40` stores the selector at `0x004B0C53` and then **zeroes the cursor** at `0x004B0C56 MOV dword ptr [EBP+0x58],0x0`, with `EBP` the class biased by 4 (`0x004B0C88 LEA ESI,[EBP-0x4]`), so those are the selector at `+0x58` and the cursor at `+0x5C`. A forced curve therefore occupies `points[0]` first, exactly as a fresh one does, and does **not** start at the RawTrack entry point. VERA's forced install left `point_index = entry_index` with the flag clear, so it took D2's terminal credit without D1's cursor fix and skipped `points[0]` - the same defect, still live on that path. **Fixed:** `begin_forced_turn_track` now sets the pre-start state. Every wired forced index selects a raw track with `entry_index == 0`, so the skip was a precise one point. A welcome consequence: the `gsi_04_05` guard that had to be weakened to `== 0` because the credit landed the arithmetic on zero now reads 1 again, so the original `!= 0` assertion is **restored** alongside the pinned value. (q) **The flagship movement fingerprints do not exercise drive tracks at all.** `s2_dense_scenario_position_fingerprint_stable` and `global_skirmish_replay_is_deterministic_and_baseline_stable` both passed **unchanged** through a 6-8 percent per-cell pace change over 20 movers and 300 ticks. That is the reason this increment lands with no integration coverage, and it is a gap in the instrument, not a reassurance. **The verification pass also found four consumers the mirror change moved silently, all uncovered.** (r) `TrackProgress::cursor` shifting by +1 corrected three further spellings of the same gate besides the one that was fixed deliberately: `at_coord.rs:113` and `track_host.rs:772-773` (the `0x004B49B6` handoff gate), and - the one that matters - `drive_track.rs:3518 occupant_can_use_track`, which is `DriveLocomotionClass::Can_Use_Track 0x004B4B00` and reads the **stored** cursor from outside the loop (`0x004B4B7A MOV EDX,[EDI+0x58]` / `0x004B4B80 CMP [ESI+0x7E7A2C],EDX` / `JNZ`, then `TEST EDX,EDX / JZ`). Before the fix the mirror fed it `cursor - 1`, so that gate fired **one paid point late**; it is right now, but it became right silently. Its twin is `world/bridge_repair_admission.rs:951-959`. None of this is visible to the suite: `at_coord_tests.rs:85-105` and `locomotor_can_use_track.json` inject `cursor` from the oracle fixtures, so they pin the **functions**, not the mirror's meaning - four occupation-claim behaviours changed with zero coverage, on top of (q). (s) **A negative residual is not swept.** `drive_locomotion.rs:38 drive_requires_native_step` tests `residual != 0`, which a negative satisfies, and `clear_selector` preserves the residual past completion, so a track-11-style ending would leave the mover "requiring a native step" for about fifteen ticks. There is no native citation anywhere for `!= 0` rather than `> 0`. (t) `world_hash.rs:347` still omits `before_first_point`; the drive path's distinction survives incidentally because the locomotion runtimes are hashed whole and now carry `occupied_points` in `cursor`, and the forced path now sets the flag too, so that incidental cover wants re-checking rather than relying on. (u) **Pre-existing, not introduced here:** `is_at_coord_track_cells` has no short-variant guard where native's `Is_At_Coord` bails out entirely on a non-zero short byte (`0x004B4967 MOV AL,[ESI+0x5C]` / `TEST AL,AL` / `JNZ`). Latent: every production `begin_forced_turn_track` caller passes `use_short = false`, and ordinary curves install from `turn.normal_track`. |

## Criteria examined without a dedicated increment

Frequency in these rows is labelled by what it rests on, because the three
are not equally strong: **STRUCTURAL** means the divergent branch sits on a
path every mover takes, so the frequency is that path's own; **STOCK DATA**
means counted from the shipped `RULESMD.INI`; **MEASURED** means counted from
real map terrain by a named probe; **NEEDS A RUN** means genuinely
behavioural, with the instrument that would settle it named.

Every criterion here has now been read against gamemd. A4 does have a landed
increment (I3); its row records what I3 did **not** cover.

| Criterion | Status | Instrument state |
|---|---|---|
| A1 Order admission | **EXAMINED 2026-09-16 - PARTIAL.** Ten divergences. The criterion's own wording was wrong and is corrected above: retail refuses almost nothing, so what matters is where a doomed order is **retargeted**. Native does retarget into the mover's own zone (`GetZoneID(ownCell)` at `0x004DE506` feeding FNPC at `0x004DE528`) - the open question in `movement_path.rs` was which cell it then picks, and that is now settled and VERA is wrong: native zeroes the selection reference at `0x004DE4D7`/`0x004DE4DC`, `0x00ABD480` reads `{0, 0}`, and the tail of `0x0056DC20` therefore takes `candidates[frame_counter % N]` - a **frame-counter rotation**, where `resolve_reachable_move_goal` passes `target_cell: Some(goal)` and takes the **nearest to the click**. **The structural finding matters more than the ten:** VERA already contains a faithful port of `0x004DE1D0` - `resolve_walk_cell_click` in `src/sim/world/move_cell_input.rs`, with the frame-counter selection, the height gate, the native anchor gate, the shroud drop and exact `Can_Reach_Zone` - but it is gated to `LocomotorKind::Walk`. **Every stock vehicle** runs a different mechanism instead: a radius-12 input adapter, then `resolve_requested_move_goal(.., 10)`, then the post-A* `resolve_reachable_move_goal`, with no shroud gate, `check_height: false`, an `UnverifiedCompatibilityBypass` anchor gate, and `ZoneGrid::can_reach`'s adjacency widening where native requires exact zone-id equality. So closing A1 means **extending the native receiver to Drive and Hover**, not tuning `resolve_reachable_move_goal`. | Eight UNCHECKED items, each naming its instrument. Report: [A1 examination](../research/MOVEMENT_ACCEPTANCE_A1_ORDER_ADMISSION_EXAMINATION.md). **Frequency.** The receiver mismatch is **STRUCTURAL and continuous**: `ordinary_ground_walk_cell_input` returns `None` for anything that is not a grounded Walk mover, so **every** order to any of the 80 stock VehicleTypes runs the substitute chain - and so do the five non-Walk infantry types and any high or falling Walk mover. The 80 is the gate's reach, not the player's: 39 of those are `TechLevel=-1` civilians and two (`DeathDummy`, `YDUM`) carry no `Locomotor` at all, so the orderable population is nearer 40, which is also what the remediation sentence above means by extending the receiver to Drive and Hover. The retarget-choice divergence within it (frame-counter rotation versus nearest-to-click) fires only on the orders that actually need a substitute, i.e. a click on an unreachable or zone-mismatched cell; that share is behavioural and **NEEDS A RUN** - a retail-map `Command::Move` trace over clicks that miss, which the existing harness could carry but no case does. **Trigger.** Two, and they nest. The receiver mismatch triggers on **every move order issued to any non-Walk mover**, because `ordinary_ground_walk_cell_input` returns `None` for anything that is not a grounded Walk mover and the substitute chain runs instead. The retarget-choice divergence inside it triggers only on the subset that actually needs a substitute - an order whose clicked cell is unreachable or in another zone - where native takes `candidates[frame_counter % N]` and VERA takes the nearest to the click. |
| A3 Drive turning and tracks | **EXAMINED 2026-09-16 against `Process_Drive_Track 0x004B0F20`.** Curve selection and the ROT contract match; the **residual budget does not**, and the `Accelerates` ramp is missing four arms. **Matching (native read, Rust read):** selector `from*8 + to` (`0x004B4016`), the `from*9` null fallback (`0x004B402E`), the two-node flag bit 3 (`0x004B403A`), the normal/short variant byte (`0x004B4019`), TurnTrack 0-7 and RawTrack 0-5 byte-for-byte, the facing quantizer (`0x004B22DC`), the exact-facing rotate-first gate (`0x004B3408`, **zero** tolerance - not a ROT band), the in-curve facing snap through `FacingClass::UpdateFacing 0x004C9300` with no ROT limiting, the per-tick budget arithmetic and step count (`0x004B1287`, `0x004B159D`, `0x004B1F64`), and three of the seven ramp arms. **D1 (worst) - fresh-curve cursor off by one.** Native inits the cursor to 0 at `0x004B4659`, reads `points[cursor]` at the loop head `0x004B1596` and increments at the tail `0x004B1F4F`; the pre-loop block `0x004B1540..0x004B158D` falls through to that read, so the first paid point is `points[0]`. VERA's production fresh selection is `track_head::begin_fresh` (`track_head.rs:73`, called from `movement_commands.rs:1066`, `movement_step.rs:224` and `:1529`, and `movement_tick.rs:888`), which sets `point_index = 0`; `begin_selected_drive_track` sets the same thing but has exactly one caller in the tree and it is a test (`drive_track_tests.rs:1272`), so an earlier version of this row cited the wrong function. Either way `advance_drive_track_with_budget_mode` (`drive_track.rs:4143-4144`) increments **before** reading, so `points[0]` is never occupied and each curve ends one point early. Trigger: every freshly selected curve - the first cell of every order, every cell where the exact-facing gate forced a turn first, every cell where chaining was declined. Frequency: continuous, every Drive and Ship mover, every match. Effect: a ground vehicle crosses a cell for 154 budget where gamemd spends about 163, roughly 5-6 percent too fast, and sits one track point (about 11 leptons) further along the arc at every intermediate tick. VERA's **chained** path already compensates by storing `entry_index - 1`; only fresh selection does not. **D2 - terminal budget credit missing.** Native adds `ftol((1.0 - manhattan/11.0) * 7.0)` back to the budget at the terminal point, `0x004B1FD0..0x004B1FF9`, constants `0x007E7FB0` = 7.0, `0x007E7FB8` = 1/11, `0x007E1718` = 1.0 (all three read from the binary). `finish_shared_track` (`movement_step.rs:1732-1760`) snaps for free. Same trigger and frequency as D1; worth up to 7 budget per cell, and correcting D1 alone would leave the per-cell cost short by this term. **D10 - residual reset is narrower** (and narrower than this row first said: the two jumps into `0x004B25F2` show an **early-exit precondition** at the top of `Process_Drive_Track`, not a per-frame reset). `0x004B0F2C` reads loco `+0x63`; a clear flag, or a set flag with selector `+0x58` == -1, reaches `0x004B0F3C CMP owner+0x5E0,8`, and `0x004B0F43 JNZ 0x004B25F2` zeroes loco `+0x4C` unless the owner is in state 8. Separately `0x004B0F49` tests loco `+0x62`, and when set a clear byte `+0xCA1` behind `CALL [vtable+0x84]` lands on the same zeroing. VERA zeroes only on path exhaustion, so it carries up to 7 stale budget through each in-place turn. **D3 - second brake band missing** (`0x004B10FC..0x004B1141`, `rawSpeed * 0.0015`, floor 0.1), gated on `owner+0x3CD`, which the examination settled as the **sinking/falling latch** from its five writers (`0x0054CEB7`, `0x00629C69`, `0x0071896B`, `0x00718AC2`, `0x00737E51`). Trigger: wrecks sinking, chrono and Temporal warps, jumpjet touchdown. **D4 - crush clamp missing** (`min(target, 0.2)` with write-back to `drive+0x50`, `0x004B1143..0x004B1173`); 15 stock `Crusher=yes` types accelerate, so this is common. **D5 - and a doc correction.** On `drive+0x58 >= 0x40` or `Passive=`, native jumps to `0x004B126F`, **past** the `SetSpeedFraction` at `0x004B1269`: it makes no call at all. The `drive_track.rs` header's claim that it sets the owner fraction directly describes `Process_Movement 0x004B3DFA` instead. **D6 - convoy propagation absent** (`0x004B1218`, `owner+0x6C8`): confirmed created only by `ScenarioClass::Read_Units_Section`, so **zero frequency in skirmish**, campaign only. Recorded, not scheduled. **D7 and D8** - native's brake distance is 3-D to `drive+0x34` with the destination ground Z where VERA uses 2-D cell distance, so slope and bridge arrivals brake a few frames late; and VERA writes `drive+0x50` unconditionally. **UNCHECKED, with the instrument each needs:** the 5-6 percent figure is arithmetic over the read track data, **not** a capture, and needs a retail motion-over-time capture or a Unicorn budget harness (neither exists); TurnTrack 8-71 and RawTrack 6-15 are unsampled (cheap memory reads); the native ramp defaults at `0x007124A8` and `0x007124C9` are unread, and `object_type.rs:286` says 0.02 where `:1656` parses 0.002; the predicate around `0x00737E51`; and `SHIP_MAX_SHARED_RAW_TRACK = 13` is VERA-internal and unproven against Ship's own `Process_Movement`. Report: [A3 examination](../research/MOVEMENT_ACCEPTANCE_A3_DRIVE_TURNING_EXAMINATION.md). I14 carries the fix for D1, D2 and D10. | Oracles consumed by `drive_track_tests.rs`; **no retail motion-over-time capture exists**, which is why every timing claim above is native-established and Rust-read but not parity-demonstrated. |
| A4 Column following (beyond I3) | **EXAMINED 2026-09-16 - NOT ACCEPTED.** The classification half matches; the **response** half does not. Matching, native-established across `0x0073F0A0..0x0073FD45`: the alliance gate (`0x0073F8C7`), the head-on exit arithmetic (`0x0073F8D4..0x0073FA26`), the in-transit and infantry skip with its ordering (`0x0073FA2C..0x0073FA7C`, running-max raise at `0x0073FA6D`), and `Can_Use_Track`, which is parity demonstrated by the existing 12,320-case oracle. A follower does chain onto a genuinely in-transit leader. **Two load-bearing divergences I3 did not cover.** (1) **The "is the ally moving" test.** Native is a three-term disjunction behind a Foot test - `Destination` (`0x0073F86B`), `FacingClass::Is_Rotating` (`0x0073F87F` into `0x004C9480`, body read to corroborate the label), and locomotor slot `+0x10` (`0x0073F8A5`). VERA uses `movement_target` alone plus a VERA-only `next_cell != pos` gate in the A* lane, so a **rotating or coasting** ally is misclassified stationary, answers code 6, and gets **scattered** - which is precisely the A4 failure mode. Frequency UNCHECKED. (2) **Retail never scatters on code 2 - FIXED 2026-09-16 (PR #392).** VERA's code-2 arm no longer nudges the blocker, and arms its wait once from `BlockagePathDelay` through `handle_blocked_tick`, which is now that timer's only writer; the arm used to pre-raise `path_blocked` to suppress it and re-implement the same arming with the 10. Removing the nudge also retired the peer-path re-snapshot that existed only because the scatter mutated the world mid-tick. **The proof was strengthened in review:** a forward control-flow walk from `0x004B364D` reaches 301 instructions with no indirect branches and none of the four `Scatter_Objects` sites, and backward closure from each site independently excludes the code-2 entry. An earlier argument here - that `0x004B393A` "ends `JMP 0x004B3607`, before the code-2 test, so it belongs to an earlier arm" - was **unsound**: it reasons from where a block jumps to about what it is entered from, and `0x004B3607` in fact falls through into `0x004B364D`, so that scatter is a **predecessor** of the code-2 test. A native pass can scatter in the bridge/height arm and then run the code-2 arm; what is true is only the reachability claim. **Consequence now stated:** nothing nudges anyone on code 2, so a mutual code-2 stall simply persists - which is native-correct, gamemd having no escape hatch there either; recovery is repath at urgency 1, urgency 2 after the span, then the stuck counter. **Still open on this row:** the `FriendlyStationary` arm (`movement_occupancy.rs`) writes the same `POST_SCATTER_WAIT_FRAMES` into `blocked_timer` citing the same retired belief. `0x004B3285` follows the scatter at `0x004B327D`, which is gated on `[esi+0x140] & 0x100` plus a height delta above two - a bridge/height arm, not a friendly-blocker one - so that arm needs tracing rather than this one's conclusion transplanted. Original evidence: established by exhaustive search: `Scatter_Objects 0x00481670` has exactly four sites in `Process_Movement` and code 2 jumps past all of them at both dispatches, leaving only a `Find_Path` with urgency raised 1 to 2. VERA scatters at `movement_occupancy.rs:1177`. **This corroborates A5's D1 and D2 from a different direction** - two examinations, one reasoning from the code-2 arm and one from the scatter call sites, reaching the same place. A4 also settles the constant at its root: the 10 in `POST_SCATTER_WAIT_FRAMES` comes from `Foot+0x64C = 0xA` at `0x004B3285`, which the binary shows is a **retry-counter reload** consumed by the decrement at `0x004B2DC8` and reached by every code including 0 - not a wait span. Native's code-2 timer is `+0x668` fed by `Rules+0x1768` = `BlockagePathDelay` (string `0x0083D314`, ctor default 60) - which stock `RULESMD.INI` declares at line 3107 under **`[AI]`**, not `[General]` as an earlier version of this row said, while `ruleset.rs` reads it from the general section; the value is right only because the constructor default is also 60, so this is a default being confirmed rather than a read, armed **once** per episode behind the `+0x6B7` latch. Both structural frequency. **Also recorded:** the Drive selection lane drops every infantry block entry where native skips only *moving* infantry, and the head-on and code map is answered from a snapshot rebuilt only on `occupancy.generation()`, which neither rotation nor in-cell translation advances. **Doc drift:** the comment on `drive_track_chain_code2_refuses_track_without_scattering` (`movement_tick.rs:4897`) states the correct native no-scatter fact that the production arm violates, and cites `0x004B3225` for a scatter site that is actually `0x004B327D`. | UNCHECKED: the RTTI ids `0xF` and `6`; whether a retail column exercises the skip at all rather than the mask release alone (needs a `-win` capture, since `derived_min_transit_separation_leptons` is a VERA ratchet, not a native measurement); and the Ship, Walk and Hover code-2 arms, which VERA serves with one shared arm. The report proposes an I3c row. Report: [A4 examination](../research/MOVEMENT_ACCEPTANCE_A4_COLUMN_FOLLOWING_EXAMINATION.md). **Frequency.** Both divergences are **STRUCTURAL**, not occasional. The code-2 response is on the only path a blocked follower takes, so it is every code-2 block of every column, every match. The "is the ally moving" misclassification is **NEEDS A RUN**, and an earlier version of this row got its logic backwards. Native's test is the disjunction `Destination OR Is_Rotating OR loco+0x10` and VERA has only `Destination`, so a divergence needs the ally to be **rotating or coasting with no destination**. A leader turning mid-column *has* a destination, so the first term answers true in both engines and they agree - "once per leader turn per follower" described the **safe** case, not the divergent one. The real population is a mover that has cleared its destination and is still swinging or drifting, which is rarer and genuinely behavioural. (For the record, the window arithmetic was wrong too: `facing_class.rs:141` is `abs(diff) / (ROT << 8)`, truncating, so 90 degrees at ROT=5 is **12** ticks, not 6-7. And `track_host.rs` snaps a track-following hull through `UpdateFacing 0x004C9300`, which clears the timer, so a moving leader may not read as rotating at all.) **Trigger.** The "is the ally moving" misclassification triggers when an ally is rotating or coasting **with its destination already cleared** - the one state native's disjunction answers true on and VERA's single `Destination` term answers false on. The code-2 response triggered on **every code-2 block**, which is what made it structural; that half is now fixed. |
| A5 Blocked recovery (retry counter, `CloseEnough`) | **EXAMINED 2026-09-16 - NOT ACCEPTED.** Ten divergences against `DriveLocomotionClass::Process_Movement 0x004B2630`, the Walk family at `0x0075AEC0`, `FootClass::Set_Destination_Internal 0x004D96B0` and `CellClass::Scatter_Objects 0x00481670`. **D1:** VERA scatters the blocker in the code-2 arm (`0x004B364D..0x004B36EF`); native does not. **D2:** `POST_SCATTER_WAIT_FRAMES = 10` (`bump_crush.rs:1037`, confirmed present) misidentifies the `Foot+0x64C` **retry counter** as a frame wait, and displaces `BlockagePathDelay = 60` (`ruleset.rs:1371`, confirmed the stock value). **D3:** the blockage timer re-arms on every pass instead of on the latch transition. **D4:** `Foot+0x64C` is reset on orders and segment repaths native leaves alone, and **D5:** decremented on a different event, with an off-by-one (native's single decrement is the `0x004B2D68` block). **D6:** `CloseEnough` is consulted on block classes native gates behind an allied blocking object. **D7:** scatter reaches one blocker where native scatters the cell's whole list. **D8:** Drive's 9-frame PathDelay limiter is left open. **D9:** a fresh non-Walk order gets a zero-length grace instead of 60. **D10:** the `+0x6B7` latch is raised for non-code-2 blocks. **Matching:** timer arithmetic at every read site, constructor state, urgency selection, `CloseEnough` polarity, a 30-case Walk code-2 native oracle, the rules constants, the **ten-entry scatter cap** (already recorded as verified-true - do not "fix" it), and the NullCoord/force=1 premise. I6 (scatter model) remains the only increment row and covers D1 and D7 only. | Six items UNCHECKED, **including every frequency estimate**: the criterion's retail blocked run still does not exist, so no divergence here carries a measured frequency. Report: [A5 examination](../research/MOVEMENT_ACCEPTANCE_A5_BLOCKED_RECOVERY_EXAMINATION.md). **Frequency, by kind.** D2 is **STRUCTURAL for non-Walk movers**: both `POST_SCATTER_WAIT_FRAMES` sites (`movement_occupancy.rs:899`, `:1210`) are gated `mover_loco_kind != LocomotorKind::Walk`, so infantry never reach it, but every blocked episode of every vehicle does. Note what the error is and is not: 10 is a **retry count** and 60 is a **frame span**, so calling it "6x" - as an earlier version of this row did - repeats the very conflation D2 exists to report. The defect is that a counter is being used as a wait, and that `blockage_path_delay_ticks` (which is live at eight call sites) is not the value consulted here. D9 is likewise every fresh non-Walk order. D1, D3, D7 and D10 sit on the code-2 arm, so they share A4's structural frequency. D5's off-by-one needs the event it hangs on to occur, and D6's `CloseEnough` widening depends on the block class mix - those two, and the per-match rates for all of them, **NEED A RUN**: the criterion's retail blocked run, which still does not exist. The examination's own caution that every frequency estimate in it is unmeasured stands for the rates; it does not weaken the structural claims. **Trigger, by divergence.** D2 triggers on **every blocked episode of a non-Walk mover** - the constant is consulted on each one. D9 triggers on **every fresh non-Walk order**. D1, D3, D7 and D10 trigger on every code-2 block (D1 and D7 now fixed). D5's off-by-one needs its decrement event to occur, and D6's `CloseEnough` widening needs a block class native gates behind an allied blocking object - those two are the conditional ones. |
| A6 Infantry stepping | **EXAMINED 2026-09-16 - NOT ACCEPTED.** Five divergences against the Walk family at `0x0075AEC0`. **D1 is a substrate divergence, not a detail:** there is no Walk locomotor step at all - infantry ride shared movement rather than a ported `WalkLocomotionClass::ProcessMovement`. The tree already half-records this: `locomotor.rs:56` says the "7-state machine matching WalkLocomotionClass (+0x50)" claim **is refuted**. **D1a:** facing source and granularity differ. **D2:** crate pickup at head commit is absent. **D3:** the speed fraction is not zeroed when the path queue empties on arrival. **D4:** the blocked-latch clear cadence differs. **D5:** the boundary tail call `0x00486920` is missing. A documentation correction is owed as well. **Matching** is recorded in the report's section 1, and the `walk_*` oracles and retail Walk crossings in the bridge harness still pass. | Instruments unchanged: `walk_*` oracles consumed, retail Walk crossings pass in the bridge harness. The report lists UNCHECKED items with the instrument each needs. Report: [A6 examination](../research/MOVEMENT_ACCEPTANCE_A6_INFANTRY_STEPPING_EXAMINATION.md). **Frequency.** D1 is **STRUCTURAL**: there is no Walk locomotor step at all, so every move of every **Walk** infantry type runs the shared path rather than a port of `0x0075AEC0`. **STOCK DATA:** that is **60 of the 65** InfantryTypes - `JUMPJET` and `LUNR` carry the Jumpjet locomotor and `CCOMAND`, `CIVAN` and `CLEG` carry `{4A582747-...}`, so five are not on Walk at all. An earlier version of this row said 65. D1a, D3 and D4 ride that same path and inherit it. D2 (crate pickup at head commit) fires only where a crate sits on a committed head cell, which **NEEDS A RUN** to rate. **Trigger.** D1 triggers on **every move of every Walk infantry type** - there is no Walk locomotor step to take, so the shared path runs unconditionally. D1a, D3 and D4 ride that same path and share its trigger. D2 triggers only where a crate sits on a committed head cell. |
| A7 Crush (beyond I4, I9a and I9c) | **EXAMINED 2026-09-16 - NOT MET.** The **kill** mechanism matches; the **admission** half of unit crushing does not, and the landed increments never covered it - I4 is overlays only, and I9a/I9c are the wall arm and `CrushCapability::of`, never the occupant crush latch `0x0073FB2A..0x0073FB6C` or its tail `0x0073FC24..0x0073FD43`. **D1, the real gap, routine frequency (re-verified here, not taken from the report):** **The governing branch is in `Can_Enter_Cell` itself**, at `0x0073F8C0..0x0073F8CE`: `MOV ECX,[EBX+0x21c]` (the occupant's house), `PUSH ESI`, `CALL HouseClass::Is_Ally_ByObject 0x004F9A90`, `TEST AL,AL`, `JZ 0x0073FAFF` - and only the **not-ally** fall-through at `0x0073FAFF` reaches the crush latch at `0x0073FB2A` at all. An earlier version of this row cited three later sites as if they were three witnesses; they are not. `Is_Crushable_By 0x005F6CD0` carries the same test in each of its two arms (omni at `0x005F6D1A`, ordinary at `0x005F6D67`) - two arms of one function, not two sequential asks - and the re-test at `0x0073FB53` is **unreachable as true**, since the same predicate on the same operands has already answered false upstream. So an **allied or own** crushable infantryman never latches, and native answers code 6 or 2. VERA's `CrushTarget` has **no owner field** and `can_crush` takes **no ally term** (both confirmed in the tree); neither `collect_crush_victims` nor `cell_passable_after_crush` adds one, so `cell_entry` returns `Crushable`. The ally test exists only at the kill site, which then refuses. **Net player effect: a tank parks on its own GI and neither crushes nor scatters it.** Untested. **D3 settled two UNCHECKED items** the `cell_entry.rs` header carried: `FUN_0040DD20` is a Techno downcast and `+0x220` is CloakState (`StartUncloaking 0x007036C0` writes 3 over 1/2), so **code 1 is a cloaked non-allied Techno**, priced 1000x in `0x0081870C`. Previously unrecorded: native's crush tail returns **2** when the cell's vehicle-occupation bit is set and `FindFirstUnit 0x0047EBA0` gives null or a non-crushable unit. **Identities:** the kill site is `UnitClass::Crush_Cell` = vtable `+0x534` (`0x007416A0`; Ghidra mislabels it `PerCellProcess`), called scatter-mode from `0x004B3E65`/`0x006A34B4` and crush-mode from `0x0073B089`. `+0xC94` is `IsTrain=` with **zero stock occurrences**, which makes the 10000-damage co-occupant loop `0x004B1864..0x004B1978` **stock-dead**; `+0xEC6` is `VehicleThief=`, also zero. **D5:** VERA's three kill lanes feed two different crusher coordinates into the 128-lepton gate. | **No parity instrument exists for A7** - no oracle and no retail-harness case; `emulate_function` on `0x005F6CD0` is the buildable candidate. The report proposes an I15 row. Report: [A7 examination](../research/MOVEMENT_ACCEPTANCE_A7_CRUSH_EXAMINATION.md). **Frequency.** **STOCK DATA:** 29 of the 80 shipped VehicleTypes carry `Crusher=yes` - `BFRT` carries `OmniCrusher=yes` **as well**, so it is one of the 29 and not a thirtieth, which an earlier version of this row implied - and 48 of the 65 InfantryTypes are crushable, the 17 exceptions carrying `Crushable=no`. Those two counts bound the reach rather than multiply into it: 39 of the 80 vehicles are `TechLevel=-1` civilians a player never orders, and two of the 48 are normally airborne, so "29 x 48 pairs" would overstate it. The **STRUCTURAL** core is what matters and is unaffected by the counting: the latch had no ally term at all, so **every** such encounter took it. How often a player drives over their own infantry is the only behavioural part and **NEEDS A RUN**. **Residual, recorded not fixed:** `Is_Ally_ByObject 0x004F9A90` is **one-directional** - identity, house-index equality (`+0x30`), then `1 << otherIndex & crusherHouse->Allies (+0x5788)`, read from the **crusher's** house only - while `are_houses_friendly` ORs both directions. It does not bite today because `HouseRoster::alliance_map()` symmetrizes at parse and nothing mutates alliances at runtime outside tests, and `is_allied_with` is documented as the helper for the asymmetric answer. So the gate matches the kill site rather than matching native exactly, and that is the shape to revisit if one-way pacts ever become mutable. **D1 FIXED 2026-09-16** (`CrushAllyGate` in `bump_crush.rs`, threaded into both admission paths at their `cell_entry` callers, reusing the kill site's own `are_houses_friendly` so the two cannot disagree; own-house settled by an interned-id compare to keep two allocations out of a per-occupant loop). The suite passed **unchanged** with the gate in place, which is how we know nothing covered it; three tests now do. |
| A8 Height, slopes and bridges (beyond I1) | **EXAMINED 2026-09-16 - NOT ACCEPTED**, seven divergences. The **height producer itself is a faithful port** and partly parity-demonstrated; what is wrong sits around it. **Matching:** the Z cadence - a paid track point and arrival resample the surface at the mover's exact sub-cell XY through `[vtable+0x1CC]` into `FootClass::Set_Height_On_Bridge 0x005F5FA0` at `0x004B1A96`, while the residual tail `0x004B2543..0x004B25A7` makes no such call and keeps the previous raw Z - mirrored by `track_host.rs:371`/`:452`/`:690` down to the `Object+0x74` save and restore, with the slot proved by base plus offset (`0x007F5C70 + 0x1CC = 0x007F5E3C`, `0x007EB058 + 0x1CC = 0x007EB224`) rather than by label. The ground evaluator `0x0047B3A0` is **parity-demonstrated** by 158 Unicorn fixtures in `tools/ramp_height_vectors.json`. Also matching: the on/off-bridge predicate (`0x004B1807..0x004B1847`), `check_bridge_traversal` arm for arm against `0x004D9C60` including the one-level ramp rule, the 3-frame tilt timer off `cell+0x11C`, and the speed chain's order and constants. **D1, the worst and new:** native decides uphill from downhill using two `GetGroundHeight` samples - the destination cell's **centre** against the ground under the mover's **exact XY** (`0x004B3CEC` and `0x004B3D1A`, compared at `0x004B3D21`) - where `terrain_speed.rs:238-258` compares the two cells' integer `level` bytes. Trigger: every ramp exit onto a flat cell at the ramp's own level. Effect: x1.0 where gamemd applies `Tracked/WheeledDownhill = 1.2`, visible only on land rows below about 0.83 because `SetSpeedFraction 0x004D3710` clamps the rest away. Frequency UNCHECKED. **On the brake distance:** it is the **same defect** as A3's D7, one site and one owner, so D7 keeps it - but A8 adds two facts A3's row lacks: native **recomputes** the destination Z as `ground(destXY)` plus the bridge offset when stamped, rather than reading the stored destination, and VERA's water-mover `+416` (`movement_tick.rs:2262`) is an **invented substitute** - additive, keyed on the **source** cell, water-only - pinned by a test asserting 516 and "outside braking range" where native gives about 428 and inside. **I1's boundary:** it landed the under-span ground land row for every ground mover with real retail coverage; it did **not** thread the numeric path height into `+0x1AC` (`cell_entry.rs:740-760` says so), did not connect the over-span destination bump to any consumer, and did not touch height legality. | **The instrument is weaker than it looks, and this is worth acting on:** A8's own retail harness asserts `entity.position.z`, a coarse **level** byte written only by `movement_bridge.rs:278`, which the drive-track crossing arm never calls, while the renderer and native parity both use `exact_z_leptons`. So the 30 retail crossings validate the fallback field at level granularity and **cannot see** a wrong lepton Z or a wrong ramp interpolation. Adding both fields to `TickRow` is a one-line fix. Report: [A8 examination](../research/MOVEMENT_ACCEPTANCE_A8_HEIGHT_SLOPES_BRIDGES_EXAMINATION.md). **Frequency.** **D1 FIXED 2026-09-16** (PR #391): `compute_cell_speed_modifier` takes the mover's exact world XY and `slope_factor_for` compares two sampled ground heights through `cell_floor_height` (`CellClass::ComputeGroundHeightAtCoord 0x0047B3A0`, already pinned by 158 Unicorn fixtures), instead of comparing two cells' `level` bytes. The review confirmed the native reading independently, including that `0x004B3CEC` is a two-call chain whose `z` is discarded and recomputed - so sampling the destination **cell centre** is right - and that neither side adds bridge deck height. It also killed the first test as unfalsifiable (the slope term is `.clamp(0, maximum * 256)`, so a ramp sample is never *below* its flat base, making the assertion a theorem); the replacement drives the production entry point over a real grid and was checked to fail when the comparison is reverted to level bytes. D1 was **STRUCTURAL per traversal and MEASURED**. It fires on every ramp exit onto a flat cell standing at the ramp's own level, and it is silent, because `SetSpeedFraction 0x004D3710` clamps the error away except on land rows below about 0.83. An earlier version of this row filed the per-map count as needing a run; that was wrong twice over - it is **static terrain**, not behaviour, and the maps were reachable all along at the `ra2_dir` this checkout's `config.toml` already names. Counted by `retail_ramp_exit_onto_equal_level_flat_census` (`bridge_restamp_retail_probe.rs`, `#[ignore]`, so its numbers come from a separate `--ignored` run and are **not** part of the suite count quoted elsewhere) over the three maps the movement harness uses: 1731 of 3956 on BayOPigs (43.8%), 237 of 490 on Hills (48.4%) and 326 of 670 on Deadman (48.7%). **Read that carefully, because it is close to tautological:** a ramp at level L rises to L+1, so its flat neighbours divide between L and L+1 roughly evenly, and a result near half largely describes what a ramp *is*. It also counts static adjacency **pairs**, not traversals. So the census establishes that the shape is **ordinary rather than rare**, and the load-bearing claim stays the structural one - `slope_factor_for` compares raw level bytes (`terrain_speed.rs`), so **every** exit of this shape diverges. How often one is driven still depends on traffic. UNCHECKED: that `ResolvedTerrainCell::level` is the ramp's base rather than its top; if it were the top the census counts uphill exits, the opposite shape. The brake-distance item is A3's D7 and carries that row's frequency. **NEW RESIDUAL from D1's review - the cliff override, and it is a larger effect than D1 was.** Native compares cell **level bytes** in the stage *before* the slope coefficient: `0x004B3589` fetches the destination cell, `0x004B358E MOVSX ECX,byte [EAX+0x11B]` reads its Level, `0x004B3595..0x004B359C` computes the absolute difference against the mover's cell level plus 4 when on a bridge, and `0x004B359E CMP EAX,2` / `0x004B35A1 JGE 0x004B3C84` forces the land-type row to **1** (`0x004B3C88 MOV ESI,1`) instead of the cell's own `+0xEC` (`0x004B35D0`). `terrain_speed_factor` has no equivalent. **Trigger:** any step whose cell level differs from the mover's by two or more - a cliff edge, and a bridge mover counts four levels higher, so stepping off a deck qualifies. **Effect:** native forces a full-speed row where VERA reads the cell's own, which on slow terrain is a far larger swing than the 1.2 the slope coefficient carries. **Frequency:** UNCHECKED and countable the same way D1 was - a level-difference census over the retail maps, which `retail_ramp_exit_onto_equal_level_flat_census` is already the pattern for. **Also recorded:** the D1 census overstates *player-visible* frequency, because `SetSpeedFraction 0x004D3710` clamps the combined value at 1.0 (`FCOMP [0x007E1718]`, `TEST AH,1`), so a full-speed destination row hides the coefficient entirely; the census counts ramp-to-flat adjacencies, not adjacencies whose destination row is low enough for the bonus to survive. And `0x004B3D1A`'s exactness is the justification for sampling the mover's sub-cell position, **not** the census: on most ramps the ramp cell's own centre already evaluates above the flat neighbour, so sampling through the height evaluator is what fixes the reported shape. |
| A9 Hover motion | **EXAMINED 2026-09-16.** Phases 2 and 3 are in production and matching mechanisms carry native addresses (the `0x2000` turn threshold, the `ftol` step derivation, the 0xFF/0x100 proximity arm, the boost and its post-boost clamp, the `1/(rate x 900)` ramps, and `FUN_00513D20` for the vertical). Two divergences are open and have their own increment rows rather than living here: **I4b**, the hard-turn translation - retail keeps translating through a corner while it brakes where VERA hard-stops - and the cruising look-ahead, where `SpeedUpdate` aims at Head_To plus one cell along the queued next direction (`0x00516211`/`0x00516230`/`0x00516241`) while VERA aims at the current waypoint. See **I4b** and **I7** for the detail, including the correction that the stall is reached through three gates and not one. | Four UNCHECKED items with their instruments, listed in I7. Report: [A9 examination](../research/MOVEMENT_ACCEPTANCE_A9_HOVER_EXAMINATION.md). **Frequency.** **STOCK DATA:** exactly 4 shipped vehicles use the hover locomotor CLSID - `SAPC`, `LCRF`, `YHVR` and `ROBO` - counted from this checkout's RULESMD.INI. This is the **same evidence** I4b already cites, re-read, not an independent confirmation of it; an earlier version of this row claimed the latter. `ROBO` is the one that matters: TechLevel 2 Allied, so it is a mainline unit rather than a curiosity. Within those four, I4b's trigger is every corner wider than 45 degrees taken while moving, bounded above as I4b's row records - the two unidentified gates mean the realised share **NEEDS A RUN**. |

## Residuals carried

- No runtime (in-game, `--release`) acceptance run was made for any row: the
  desktop-control request needed for a visual pass was declined this session,
  so I4 (crush sound), I3b (hover in transit) and I9a (sandbag refusal) are
  harness-tested only.
- The retail module's `bridge_tile_retail_probe` tests skip when the campaign
  maps are not extracted, and `bridge_restamp_retail_probe` writes its
  inventory under `target/` (or `VERA20K_RESTAMP_OUTPUT`) since the audit fix;
  before it they were three environment reds in a full ignored run. A SKIPPED
  probe counts as a pass, so this line is the record that they did not run.

- I1 consequence: beneath a collapsed high span the structural stamp is kept
  while the deck is no longer elevated, so Drive is admitted on the ground plane
  by the same rule as beneath an intact span. This follows the native reading
  (`0x004D9C60` and the land row decide; `0x100` only selects the plane) but no
  retail damaged-span crossing has been run for Drive.

- Attack-move characterizations `*_attack_moved_across_*_is_currently_dropped`
  were rewritten as positive crossings (`*_attack_moved_across_*_crosses`,
  2026-09-15) after the probe showed every mover crossing; which change on
  `main` lifted the stall was not bisected.
- `bridge_tile_retail_probe` needs extracted campaign maps (`c3y03md.map`,
  `xmp34u4.map`) beside the retail install (or the `VERA20K_*_MAP` variables);
  without them it reports `SKIPPED` rather than failing.

- A12 (wall-clock pace) is **structurally matched, wall-clock unmeasured**. Admission
  mirrors the native gate (`LocalFramePacer::should_admit`, `timeGetTime() >> 4`
  buckets against `game_speed.clamp(1,6)`) and each admitted frame advances exactly
  one native frame of movement (`speed × 1/15`), but no measurement was taken on
  either side, so this is a code-and-binary read and not parity demonstrated. A live
  capture of both engines at `GameSpeed=1` is what would move it.
- Live, and separate from A12's derivation: `render/gpu.rs:184` pins
  `wgpu::PresentMode::Fifo`, and frame admission is reachable only from the render
  pass (`app/frame.rs:142`) with no catch-up loop — admission only ever *permits* a
  frame and never replays a missed one. Achieved logic rate is therefore
  `min(display refresh, 62.5/s)`. Trigger: any display below ~63 Hz, which is most.
  Effect: a uniform pace deficit across every frame-counted system — ~4% at 60 Hz,
  2× at 30 fps, appearing as slow motion rather than gamemd's frame-dropping.
  Frequency: continuous. Downstream risk: any future wall-clock parity measurement
  reads this ceiling rather than the pacer.
- Deferred, both fully evidenced and neither authored by the increment that found
  them (2026-09-16). First: the **I9c row above is malformed** — it carries seven
  unescaped pipes in a five-pipe table, because the Rust expression
  `omni_crusher || MovementZone in {Crusher, AmphibiousCrusher, CrusherAll}` was
  transcribed with a bare `||` at offsets 554/555, so the row renders with two
  spurious columns. Fix is `\|\|`. Landed in `47f769b6` (PR #380). Second:
  `src/rules/object_type.rs:281` reads "Fraction of max speed gained per tick
  (`AccelerationFactor=`). Default 0.03. **At 15 fps, reaches max speed in ~2
  seconds**". The per-tick wording is right and matches native — gamemd adds the
  factor directly to the fraction once per Drive frame
  (`FLD [ESI+0x308]; FADD [ECX+0x578]` at `0x004B11A3`, per
  `DRIVE_ACCELERATES_TRUE_FALSE_SPEED_RAMP_GHIDRA_REPORT.md:119`) — but the
  seconds gloss assumes the dead 15 fps premise that row I13 retires; at stock
  admission (~62.5 frames/s) the same 0.03 reaches full speed in ~0.5 s. That
  report already flags `object_type.rs` comment drift at its line 173.
- Verified **true** while auditing, and recorded so it is not "corrected" later:
  A5's `ten-entry cap`. `CellClass__Scatter_Objects 0x00481670` gates its
  collection loop on `FUN_0040ce50(10, 0)` and builds up to ten occupants before
  scattering them in collected order. VERA's `MAX_SCATTER_DIRECTIONS = 351` and
  `MAX_SPIRAL_RADIUS = 10` (`scatter.rs`) are the spiral *destination* search, a
  different mechanism; comparing the two is what made the criterion look wrong.
